// Copyright (c) 2025, NVIDIA CORPORATION.  All rights reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// WARNING: THIS FILE WAS AUTOMATICALLY GENERATED.
// Code generated by gen/nvml/gen-dispatch. DO NOT EDIT.

#ifndef NVML_DISPATCH_H
#define NVML_DISPATCH_H

#include "nvml.h"

// nvml_dispatch_t holds function pointers for all NVML API functions.
// Populated at library load time via populateDispatch() in Go.
typedef struct {
    nvmlReturn_t (*nvmlInit_v2)(void);
    nvmlReturn_t (*nvmlInitWithFlags)(unsigned int flags);
    nvmlReturn_t (*nvmlShutdown)(void);
    const char* (*nvmlErrorString)(nvmlReturn_t result);
    nvmlReturn_t (*nvmlSystemGetDriverVersion)(char * version, unsigned int length);
    nvmlReturn_t (*nvmlSystemGetNVMLVersion)(char * version, unsigned int length);
    nvmlReturn_t (*nvmlSystemGetCudaDriverVersion)(int * cudaDriverVersion);
    nvmlReturn_t (*nvmlSystemGetCudaDriverVersion_v2)(int * cudaDriverVersion);
    nvmlReturn_t (*nvmlSystemGetProcessName)(unsigned int pid, char * name, unsigned int length);
    nvmlReturn_t (*nvmlSystemGetHicVersion)(unsigned int * hwbcCount, nvmlHwbcEntry_t * hwbcEntries);
    nvmlReturn_t (*nvmlSystemGetTopologyGpuSet)(unsigned int cpuNumber, unsigned int * count, nvmlDevice_t * deviceArray);
    nvmlReturn_t (*nvmlSystemGetDriverBranch)(nvmlSystemDriverBranchInfo_t * branchInfo, unsigned int length);
    nvmlReturn_t (*nvmlUnitGetCount)(unsigned int * unitCount);
    nvmlReturn_t (*nvmlUnitGetHandleByIndex)(unsigned int index, nvmlUnit_t * unit);
    nvmlReturn_t (*nvmlUnitGetUnitInfo)(nvmlUnit_t unit, nvmlUnitInfo_t * info);
    nvmlReturn_t (*nvmlUnitGetLedState)(nvmlUnit_t unit, nvmlLedState_t * state);
    nvmlReturn_t (*nvmlUnitGetPsuInfo)(nvmlUnit_t unit, nvmlPSUInfo_t * psu);
    nvmlReturn_t (*nvmlUnitGetTemperature)(nvmlUnit_t unit, unsigned int type, unsigned int * temp);
    nvmlReturn_t (*nvmlUnitGetFanSpeedInfo)(nvmlUnit_t unit, nvmlUnitFanSpeeds_t * fanSpeeds);
    nvmlReturn_t (*nvmlUnitGetDevices)(nvmlUnit_t unit, unsigned int * deviceCount, nvmlDevice_t * devices);
    nvmlReturn_t (*nvmlDeviceGetCount_v2)(unsigned int * deviceCount);
    nvmlReturn_t (*nvmlDeviceGetAttributes_v2)(nvmlDevice_t device, nvmlDeviceAttributes_t * attributes);
    nvmlReturn_t (*nvmlDeviceGetHandleByIndex_v2)(unsigned int index, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlDeviceGetHandleBySerial)(const char * serial, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlDeviceGetHandleByUUID)(const char * uuid, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlDeviceGetHandleByUUIDV)(const nvmlUUID_t * uuid, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlDeviceGetHandleByPciBusId_v2)(const char * pciBusId, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlDeviceGetName)(nvmlDevice_t device, char * name, unsigned int length);
    nvmlReturn_t (*nvmlDeviceGetBrand)(nvmlDevice_t device, nvmlBrandType_t * type);
    nvmlReturn_t (*nvmlDeviceGetIndex)(nvmlDevice_t device, unsigned int * index);
    nvmlReturn_t (*nvmlDeviceGetSerial)(nvmlDevice_t device, char * serial, unsigned int length);
    nvmlReturn_t (*nvmlDeviceGetModuleId)(nvmlDevice_t device, unsigned int * moduleId);
    nvmlReturn_t (*nvmlDeviceGetC2cModeInfoV)(nvmlDevice_t device, nvmlC2cModeInfo_v1_t * c2cModeInfo);
    nvmlReturn_t (*nvmlDeviceGetMemoryAffinity)(nvmlDevice_t device, unsigned int nodeSetSize, unsigned long * nodeSet, nvmlAffinityScope_t scope);
    nvmlReturn_t (*nvmlDeviceGetCpuAffinityWithinScope)(nvmlDevice_t device, unsigned int cpuSetSize, unsigned long * cpuSet, nvmlAffinityScope_t scope);
    nvmlReturn_t (*nvmlDeviceGetCpuAffinity)(nvmlDevice_t device, unsigned int cpuSetSize, unsigned long * cpuSet);
    nvmlReturn_t (*nvmlDeviceSetCpuAffinity)(nvmlDevice_t device);
    nvmlReturn_t (*nvmlDeviceClearCpuAffinity)(nvmlDevice_t device);
    nvmlReturn_t (*nvmlDeviceGetNumaNodeId)(nvmlDevice_t device, unsigned int * node);
    nvmlReturn_t (*nvmlDeviceGetAddressingMode)(nvmlDevice_t device, nvmlDeviceAddressingMode_t * mode);
    nvmlReturn_t (*nvmlDeviceGetRepairStatus)(nvmlDevice_t device, nvmlRepairStatus_t * repairStatus);
    nvmlReturn_t (*nvmlDeviceGetTopologyCommonAncestor)(nvmlDevice_t device1, nvmlDevice_t device2, nvmlGpuTopologyLevel_t * pathInfo);
    nvmlReturn_t (*nvmlDeviceGetTopologyNearestGpus)(nvmlDevice_t device, nvmlGpuTopologyLevel_t level, unsigned int * count, nvmlDevice_t * deviceArray);
    nvmlReturn_t (*nvmlDeviceGetP2PStatus)(nvmlDevice_t device1, nvmlDevice_t device2, nvmlGpuP2PCapsIndex_t p2pIndex, nvmlGpuP2PStatus_t * p2pStatus);
    nvmlReturn_t (*nvmlDeviceGetUUID)(nvmlDevice_t device, char * uuid, unsigned int length);
    nvmlReturn_t (*nvmlDeviceGetMinorNumber)(nvmlDevice_t device, unsigned int * minorNumber);
    nvmlReturn_t (*nvmlDeviceGetBoardPartNumber)(nvmlDevice_t device, char* partNumber, unsigned int length);
    nvmlReturn_t (*nvmlDeviceGetInforomVersion)(nvmlDevice_t device, nvmlInforomObject_t object, char * version, unsigned int length);
    nvmlReturn_t (*nvmlDeviceGetInforomImageVersion)(nvmlDevice_t device, char * version, unsigned int length);
    nvmlReturn_t (*nvmlDeviceGetInforomConfigurationChecksum)(nvmlDevice_t device, unsigned int * checksum);
    nvmlReturn_t (*nvmlDeviceValidateInforom)(nvmlDevice_t device);
    nvmlReturn_t (*nvmlDeviceGetLastBBXFlushTime)(nvmlDevice_t device, unsigned long long * timestamp, unsigned long * durationUs);
    nvmlReturn_t (*nvmlDeviceGetDisplayMode)(nvmlDevice_t device, nvmlEnableState_t * display);
    nvmlReturn_t (*nvmlDeviceGetDisplayActive)(nvmlDevice_t device, nvmlEnableState_t * isActive);
    nvmlReturn_t (*nvmlDeviceGetPersistenceMode)(nvmlDevice_t device, nvmlEnableState_t * mode);
    nvmlReturn_t (*nvmlDeviceGetPciInfoExt)(nvmlDevice_t device, nvmlPciInfoExt_t * pci);
    nvmlReturn_t (*nvmlDeviceGetPciInfo_v3)(nvmlDevice_t device, nvmlPciInfo_t * pci);
    nvmlReturn_t (*nvmlDeviceGetMaxPcieLinkGeneration)(nvmlDevice_t device, unsigned int * maxLinkGen);
    nvmlReturn_t (*nvmlDeviceGetGpuMaxPcieLinkGeneration)(nvmlDevice_t device, unsigned int * maxLinkGenDevice);
    nvmlReturn_t (*nvmlDeviceGetMaxPcieLinkWidth)(nvmlDevice_t device, unsigned int * maxLinkWidth);
    nvmlReturn_t (*nvmlDeviceGetCurrPcieLinkGeneration)(nvmlDevice_t device, unsigned int * currLinkGen);
    nvmlReturn_t (*nvmlDeviceGetCurrPcieLinkWidth)(nvmlDevice_t device, unsigned int * currLinkWidth);
    nvmlReturn_t (*nvmlDeviceGetPcieThroughput)(nvmlDevice_t device, nvmlPcieUtilCounter_t counter, unsigned int * value);
    nvmlReturn_t (*nvmlDeviceGetPcieReplayCounter)(nvmlDevice_t device, unsigned int * value);
    nvmlReturn_t (*nvmlDeviceGetClockInfo)(nvmlDevice_t device, nvmlClockType_t type, unsigned int * clock);
    nvmlReturn_t (*nvmlDeviceGetMaxClockInfo)(nvmlDevice_t device, nvmlClockType_t type, unsigned int * clock);
    nvmlReturn_t (*nvmlDeviceGetGpcClkVfOffset)(nvmlDevice_t device, int * offset);
    nvmlReturn_t (*nvmlDeviceGetApplicationsClock)(nvmlDevice_t device, nvmlClockType_t clockType, unsigned int * clockMHz);
    nvmlReturn_t (*nvmlDeviceGetDefaultApplicationsClock)(nvmlDevice_t device, nvmlClockType_t clockType, unsigned int * clockMHz);
    nvmlReturn_t (*nvmlDeviceGetClock)(nvmlDevice_t device, nvmlClockType_t clockType, nvmlClockId_t clockId, unsigned int * clockMHz);
    nvmlReturn_t (*nvmlDeviceGetMaxCustomerBoostClock)(nvmlDevice_t device, nvmlClockType_t clockType, unsigned int * clockMHz);
    nvmlReturn_t (*nvmlDeviceGetSupportedMemoryClocks)(nvmlDevice_t device, unsigned int * count, unsigned int * clocksMHz);
    nvmlReturn_t (*nvmlDeviceGetSupportedGraphicsClocks)(nvmlDevice_t device, unsigned int memoryClockMHz, unsigned int * count, unsigned int * clocksMHz);
    nvmlReturn_t (*nvmlDeviceGetAutoBoostedClocksEnabled)(nvmlDevice_t device, nvmlEnableState_t * isEnabled, nvmlEnableState_t * defaultIsEnabled);
    nvmlReturn_t (*nvmlDeviceGetFanSpeed)(nvmlDevice_t device, unsigned int * speed);
    nvmlReturn_t (*nvmlDeviceGetFanSpeed_v2)(nvmlDevice_t device, unsigned int fan, unsigned int * speed);
    nvmlReturn_t (*nvmlDeviceGetFanSpeedRPM)(nvmlDevice_t device, nvmlFanSpeedInfo_t * fanSpeed);
    nvmlReturn_t (*nvmlDeviceGetTargetFanSpeed)(nvmlDevice_t device, unsigned int fan, unsigned int * targetSpeed);
    nvmlReturn_t (*nvmlDeviceGetMinMaxFanSpeed)(nvmlDevice_t device, unsigned int * minSpeed, unsigned int * maxSpeed);
    nvmlReturn_t (*nvmlDeviceGetFanControlPolicy_v2)(nvmlDevice_t device, unsigned int fan, nvmlFanControlPolicy_t * policy);
    nvmlReturn_t (*nvmlDeviceGetNumFans)(nvmlDevice_t device, unsigned int * numFans);
    nvmlReturn_t (*nvmlDeviceGetTemperature)(nvmlDevice_t device, nvmlTemperatureSensors_t sensorType, unsigned int * temp);
    nvmlReturn_t (*nvmlDeviceGetCoolerInfo)(nvmlDevice_t device, nvmlCoolerInfo_t * coolerInfo);
    nvmlReturn_t (*nvmlDeviceGetTemperatureV)(nvmlDevice_t device, nvmlTemperature_t * temperature);
    nvmlReturn_t (*nvmlDeviceGetTemperatureThreshold)(nvmlDevice_t device, nvmlTemperatureThresholds_t thresholdType, unsigned int * temp);
    nvmlReturn_t (*nvmlDeviceGetMarginTemperature)(nvmlDevice_t device, nvmlMarginTemperature_t * marginTempInfo);
    nvmlReturn_t (*nvmlDeviceGetThermalSettings)(nvmlDevice_t device, unsigned int sensorIndex, nvmlGpuThermalSettings_t * pThermalSettings);
    nvmlReturn_t (*nvmlDeviceGetPerformanceState)(nvmlDevice_t device, nvmlPstates_t * pState);
    nvmlReturn_t (*nvmlDeviceGetCurrentClocksEventReasons)(nvmlDevice_t device, unsigned long long * clocksEventReasons);
    nvmlReturn_t (*nvmlDeviceGetCurrentClocksThrottleReasons)(nvmlDevice_t device, unsigned long long * clocksThrottleReasons);
    nvmlReturn_t (*nvmlDeviceGetSupportedClocksEventReasons)(nvmlDevice_t device, unsigned long long * supportedClocksEventReasons);
    nvmlReturn_t (*nvmlDeviceGetSupportedClocksThrottleReasons)(nvmlDevice_t device, unsigned long long * supportedClocksThrottleReasons);
    nvmlReturn_t (*nvmlDeviceGetPowerState)(nvmlDevice_t device, nvmlPstates_t * pState);
    nvmlReturn_t (*nvmlDeviceGetDynamicPstatesInfo)(nvmlDevice_t device, nvmlGpuDynamicPstatesInfo_t * pDynamicPstatesInfo);
    nvmlReturn_t (*nvmlDeviceGetMemClkVfOffset)(nvmlDevice_t device, int * offset);
    nvmlReturn_t (*nvmlDeviceGetMinMaxClockOfPState)(nvmlDevice_t device, nvmlClockType_t type, nvmlPstates_t pstate, unsigned int * minClockMHz, unsigned int * maxClockMHz);
    nvmlReturn_t (*nvmlDeviceGetSupportedPerformanceStates)(nvmlDevice_t device, nvmlPstates_t * pstates, unsigned int size);
    nvmlReturn_t (*nvmlDeviceGetGpcClkMinMaxVfOffset)(nvmlDevice_t device, int * minOffset, int * maxOffset);
    nvmlReturn_t (*nvmlDeviceGetMemClkMinMaxVfOffset)(nvmlDevice_t device, int * minOffset, int * maxOffset);
    nvmlReturn_t (*nvmlDeviceGetClockOffsets)(nvmlDevice_t device, nvmlClockOffset_t * info);
    nvmlReturn_t (*nvmlDeviceSetClockOffsets)(nvmlDevice_t device, nvmlClockOffset_t * info);
    nvmlReturn_t (*nvmlDeviceGetPerformanceModes)(nvmlDevice_t device, nvmlDevicePerfModes_t * perfModes);
    nvmlReturn_t (*nvmlDeviceGetCurrentClockFreqs)(nvmlDevice_t device, nvmlDeviceCurrentClockFreqs_t * currentClockFreqs);
    nvmlReturn_t (*nvmlDeviceGetPowerManagementMode)(nvmlDevice_t device, nvmlEnableState_t * mode);
    nvmlReturn_t (*nvmlDeviceGetPowerManagementLimit)(nvmlDevice_t device, unsigned int * limit);
    nvmlReturn_t (*nvmlDeviceGetPowerManagementLimitConstraints)(nvmlDevice_t device, unsigned int * minLimit, unsigned int * maxLimit);
    nvmlReturn_t (*nvmlDeviceGetPowerManagementDefaultLimit)(nvmlDevice_t device, unsigned int * defaultLimit);
    nvmlReturn_t (*nvmlDeviceGetPowerUsage)(nvmlDevice_t device, unsigned int * power);
    nvmlReturn_t (*nvmlDeviceGetPowerMizerMode_v1)(nvmlDevice_t device, nvmlDevicePowerMizerModes_v1_t * powerMizerMode);
    nvmlReturn_t (*nvmlDeviceSetPowerMizerMode_v1)(nvmlDevice_t device, nvmlDevicePowerMizerModes_v1_t * powerMizerMode);
    nvmlReturn_t (*nvmlDeviceGetTotalEnergyConsumption)(nvmlDevice_t device, unsigned long long * energy);
    nvmlReturn_t (*nvmlDeviceGetEnforcedPowerLimit)(nvmlDevice_t device, unsigned int * limit);
    nvmlReturn_t (*nvmlDeviceGetGpuOperationMode)(nvmlDevice_t device, nvmlGpuOperationMode_t * current, nvmlGpuOperationMode_t * pending);
    nvmlReturn_t (*nvmlDeviceGetMemoryInfo)(nvmlDevice_t device, nvmlMemory_t * memory);
    nvmlReturn_t (*nvmlDeviceGetMemoryInfo_v2)(nvmlDevice_t device, nvmlMemory_v2_t * memory);
    nvmlReturn_t (*nvmlDeviceGetComputeMode)(nvmlDevice_t device, nvmlComputeMode_t * mode);
    nvmlReturn_t (*nvmlDeviceGetCudaComputeCapability)(nvmlDevice_t device, int * major, int * minor);
    nvmlReturn_t (*nvmlDeviceGetDramEncryptionMode)(nvmlDevice_t device, nvmlDramEncryptionInfo_t * current, nvmlDramEncryptionInfo_t * pending);
    nvmlReturn_t (*nvmlDeviceSetDramEncryptionMode)(nvmlDevice_t device, const nvmlDramEncryptionInfo_t * dramEncryption);
    nvmlReturn_t (*nvmlDeviceGetEccMode)(nvmlDevice_t device, nvmlEnableState_t * current, nvmlEnableState_t * pending);
    nvmlReturn_t (*nvmlDeviceGetDefaultEccMode)(nvmlDevice_t device, nvmlEnableState_t * defaultMode);
    nvmlReturn_t (*nvmlDeviceGetBoardId)(nvmlDevice_t device, unsigned int * boardId);
    nvmlReturn_t (*nvmlDeviceGetMultiGpuBoard)(nvmlDevice_t device, unsigned int * multiGpuBool);
    nvmlReturn_t (*nvmlDeviceGetTotalEccErrors)(nvmlDevice_t device, nvmlMemoryErrorType_t errorType, nvmlEccCounterType_t counterType, unsigned long long * eccCounts);
    nvmlReturn_t (*nvmlDeviceGetDetailedEccErrors)(nvmlDevice_t device, nvmlMemoryErrorType_t errorType, nvmlEccCounterType_t counterType, nvmlEccErrorCounts_t * eccCounts);
    nvmlReturn_t (*nvmlDeviceGetMemoryErrorCounter)(nvmlDevice_t device, nvmlMemoryErrorType_t errorType, nvmlEccCounterType_t counterType, nvmlMemoryLocation_t locationType, unsigned long long * count);
    nvmlReturn_t (*nvmlDeviceGetUtilizationRates)(nvmlDevice_t device, nvmlUtilization_t * utilization);
    nvmlReturn_t (*nvmlDeviceGetEncoderUtilization)(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs);
    nvmlReturn_t (*nvmlDeviceGetEncoderCapacity)(nvmlDevice_t device, nvmlEncoderType_t encoderQueryType, unsigned int * encoderCapacity);
    nvmlReturn_t (*nvmlDeviceGetEncoderStats)(nvmlDevice_t device, unsigned int * sessionCount, unsigned int * averageFps, unsigned int * averageLatency);
    nvmlReturn_t (*nvmlDeviceGetEncoderSessions)(nvmlDevice_t device, unsigned int * sessionCount, nvmlEncoderSessionInfo_t * sessionInfos);
    nvmlReturn_t (*nvmlDeviceGetDecoderUtilization)(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs);
    nvmlReturn_t (*nvmlDeviceGetJpgUtilization)(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs);
    nvmlReturn_t (*nvmlDeviceGetOfaUtilization)(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs);
    nvmlReturn_t (*nvmlDeviceGetFBCStats)(nvmlDevice_t device, nvmlFBCStats_t * fbcStats);
    nvmlReturn_t (*nvmlDeviceGetFBCSessions)(nvmlDevice_t device, unsigned int * sessionCount, nvmlFBCSessionInfo_t * sessionInfo);
    nvmlReturn_t (*nvmlDeviceGetDriverModel_v2)(nvmlDevice_t device, nvmlDriverModel_t * current, nvmlDriverModel_t * pending);
    nvmlReturn_t (*nvmlDeviceGetVbiosVersion)(nvmlDevice_t device, char * version, unsigned int length);
    nvmlReturn_t (*nvmlDeviceGetBridgeChipInfo)(nvmlDevice_t device, nvmlBridgeChipHierarchy_t * bridgeHierarchy);
    nvmlReturn_t (*nvmlDeviceGetComputeRunningProcesses_v3)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_t * infos);
    nvmlReturn_t (*nvmlDeviceGetGraphicsRunningProcesses_v3)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_t * infos);
    nvmlReturn_t (*nvmlDeviceGetMPSComputeRunningProcesses_v3)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_t * infos);
    nvmlReturn_t (*nvmlDeviceGetRunningProcessDetailList)(nvmlDevice_t device, nvmlProcessDetailList_t * plist);
    nvmlReturn_t (*nvmlDeviceOnSameBoard)(nvmlDevice_t device1, nvmlDevice_t device2, int * onSameBoard);
    nvmlReturn_t (*nvmlDeviceGetAPIRestriction)(nvmlDevice_t device, nvmlRestrictedAPI_t apiType, nvmlEnableState_t * isRestricted);
    nvmlReturn_t (*nvmlDeviceGetSamples)(nvmlDevice_t device, nvmlSamplingType_t type, unsigned long long lastSeenTimeStamp, nvmlValueType_t * sampleValType, unsigned int * sampleCount, nvmlSample_t * samples);
    nvmlReturn_t (*nvmlDeviceGetBAR1MemoryInfo)(nvmlDevice_t device, nvmlBAR1Memory_t * bar1Memory);
    nvmlReturn_t (*nvmlDeviceGetViolationStatus)(nvmlDevice_t device, nvmlPerfPolicyType_t perfPolicyType, nvmlViolationTime_t * violTime);
    nvmlReturn_t (*nvmlDeviceGetIrqNum)(nvmlDevice_t device, unsigned int * irqNum);
    nvmlReturn_t (*nvmlDeviceGetNumGpuCores)(nvmlDevice_t device, unsigned int * numCores);
    nvmlReturn_t (*nvmlDeviceGetPowerSource)(nvmlDevice_t device, nvmlPowerSource_t * powerSource);
    nvmlReturn_t (*nvmlDeviceGetMemoryBusWidth)(nvmlDevice_t device, unsigned int * busWidth);
    nvmlReturn_t (*nvmlDeviceGetPcieLinkMaxSpeed)(nvmlDevice_t device, unsigned int * maxSpeed);
    nvmlReturn_t (*nvmlDeviceGetPcieSpeed)(nvmlDevice_t device, unsigned int * pcieSpeed);
    nvmlReturn_t (*nvmlDeviceGetAdaptiveClockInfoStatus)(nvmlDevice_t device, unsigned int * adaptiveClockStatus);
    nvmlReturn_t (*nvmlDeviceGetBusType)(nvmlDevice_t device, nvmlBusType_t * type);
    nvmlReturn_t (*nvmlDeviceGetGpuFabricInfo)(nvmlDevice_t device, nvmlGpuFabricInfo_t * gpuFabricInfo);
    nvmlReturn_t (*nvmlDeviceGetGpuFabricInfoV)(nvmlDevice_t device, nvmlGpuFabricInfoV_t * gpuFabricInfo);
    nvmlReturn_t (*nvmlSystemGetConfComputeCapabilities)(nvmlConfComputeSystemCaps_t * capabilities);
    nvmlReturn_t (*nvmlSystemGetConfComputeState)(nvmlConfComputeSystemState_t * state);
    nvmlReturn_t (*nvmlDeviceGetConfComputeMemSizeInfo)(nvmlDevice_t device, nvmlConfComputeMemSizeInfo_t * memInfo);
    nvmlReturn_t (*nvmlSystemGetConfComputeGpusReadyState)(unsigned int * isAcceptingWork);
    nvmlReturn_t (*nvmlDeviceGetConfComputeProtectedMemoryUsage)(nvmlDevice_t device, nvmlMemory_t * memory);
    nvmlReturn_t (*nvmlDeviceGetConfComputeGpuCertificate)(nvmlDevice_t device, nvmlConfComputeGpuCertificate_t * gpuCert);
    nvmlReturn_t (*nvmlDeviceGetConfComputeGpuAttestationReport)(nvmlDevice_t device, nvmlConfComputeGpuAttestationReport_t * gpuAtstReport);
    nvmlReturn_t (*nvmlSystemGetConfComputeKeyRotationThresholdInfo)(nvmlConfComputeGetKeyRotationThresholdInfo_t * pKeyRotationThrInfo);
    nvmlReturn_t (*nvmlDeviceSetConfComputeUnprotectedMemSize)(nvmlDevice_t device, unsigned long long sizeKiB);
    nvmlReturn_t (*nvmlSystemSetConfComputeGpusReadyState)(unsigned int isAcceptingWork);
    nvmlReturn_t (*nvmlSystemSetConfComputeKeyRotationThresholdInfo)(nvmlConfComputeSetKeyRotationThresholdInfo_t * pKeyRotationThrInfo);
    nvmlReturn_t (*nvmlSystemGetConfComputeSettings)(nvmlSystemConfComputeSettings_t * settings);
    nvmlReturn_t (*nvmlDeviceGetGspFirmwareVersion)(nvmlDevice_t device, char * version);
    nvmlReturn_t (*nvmlDeviceGetGspFirmwareMode)(nvmlDevice_t device, unsigned int * isEnabled, unsigned int * defaultMode);
    nvmlReturn_t (*nvmlDeviceGetSramEccErrorStatus)(nvmlDevice_t device, nvmlEccSramErrorStatus_t * status);
    nvmlReturn_t (*nvmlDeviceSetPowerManagementLimit_v2)(nvmlDevice_t device, nvmlPowerValue_v2_t * powerValue);
    nvmlReturn_t (*nvmlDeviceGetAccountingMode)(nvmlDevice_t device, nvmlEnableState_t * mode);
    nvmlReturn_t (*nvmlDeviceGetAccountingStats)(nvmlDevice_t device, unsigned int pid, nvmlAccountingStats_t * stats);
    nvmlReturn_t (*nvmlDeviceGetAccountingPids)(nvmlDevice_t device, unsigned int * count, unsigned int * pids);
    nvmlReturn_t (*nvmlDeviceGetAccountingBufferSize)(nvmlDevice_t device, unsigned int * bufferSize);
    nvmlReturn_t (*nvmlDeviceGetRetiredPages)(nvmlDevice_t device, nvmlPageRetirementCause_t cause, unsigned int * pageCount, unsigned long long * addresses);
    nvmlReturn_t (*nvmlDeviceGetRetiredPages_v2)(nvmlDevice_t device, nvmlPageRetirementCause_t cause, unsigned int * pageCount, unsigned long long * addresses, unsigned long long * timestamps);
    nvmlReturn_t (*nvmlDeviceGetRetiredPagesPendingStatus)(nvmlDevice_t device, nvmlEnableState_t * isPending);
    nvmlReturn_t (*nvmlDeviceGetRemappedRows)(nvmlDevice_t device, unsigned int * corrRows, unsigned int * uncRows, unsigned int * isPending, unsigned int * failureOccurred);
    nvmlReturn_t (*nvmlDeviceGetRowRemapperHistogram)(nvmlDevice_t device, nvmlRowRemapperHistogramValues_t * values);
    nvmlReturn_t (*nvmlDeviceGetArchitecture)(nvmlDevice_t device, nvmlDeviceArchitecture_t * arch);
    nvmlReturn_t (*nvmlDeviceGetClkMonStatus)(nvmlDevice_t device, nvmlClkMonStatus_t * status);
    nvmlReturn_t (*nvmlDeviceGetProcessUtilization)(nvmlDevice_t device, nvmlProcessUtilizationSample_t * utilization, unsigned int * processSamplesCount, unsigned long long lastSeenTimeStamp);
    nvmlReturn_t (*nvmlDeviceGetProcessesUtilizationInfo)(nvmlDevice_t device, nvmlProcessesUtilizationInfo_t * procesesUtilInfo);
    nvmlReturn_t (*nvmlDeviceGetPlatformInfo)(nvmlDevice_t device, nvmlPlatformInfo_t * platformInfo);
    nvmlReturn_t (*nvmlDeviceGetPdi)(nvmlDevice_t device, nvmlPdi_t * pdi);
    nvmlReturn_t (*nvmlUnitSetLedState)(nvmlUnit_t unit, nvmlLedColor_t color);
    nvmlReturn_t (*nvmlDeviceSetPersistenceMode)(nvmlDevice_t device, nvmlEnableState_t mode);
    nvmlReturn_t (*nvmlDeviceSetComputeMode)(nvmlDevice_t device, nvmlComputeMode_t mode);
    nvmlReturn_t (*nvmlDeviceSetEccMode)(nvmlDevice_t device, nvmlEnableState_t ecc);
    nvmlReturn_t (*nvmlDeviceClearEccErrorCounts)(nvmlDevice_t device, nvmlEccCounterType_t counterType);
    nvmlReturn_t (*nvmlDeviceSetDriverModel)(nvmlDevice_t device, nvmlDriverModel_t driverModel, unsigned int flags);
    nvmlReturn_t (*nvmlDeviceSetGpuLockedClocks)(nvmlDevice_t device, unsigned int minGpuClockMHz, unsigned int maxGpuClockMHz);
    nvmlReturn_t (*nvmlDeviceResetGpuLockedClocks)(nvmlDevice_t device);
    nvmlReturn_t (*nvmlDeviceSetMemoryLockedClocks)(nvmlDevice_t device, unsigned int minMemClockMHz, unsigned int maxMemClockMHz);
    nvmlReturn_t (*nvmlDeviceResetMemoryLockedClocks)(nvmlDevice_t device);
    nvmlReturn_t (*nvmlDeviceSetApplicationsClocks)(nvmlDevice_t device, unsigned int memClockMHz, unsigned int graphicsClockMHz);
    nvmlReturn_t (*nvmlDeviceResetApplicationsClocks)(nvmlDevice_t device);
    nvmlReturn_t (*nvmlDeviceSetAutoBoostedClocksEnabled)(nvmlDevice_t device, nvmlEnableState_t enabled);
    nvmlReturn_t (*nvmlDeviceSetDefaultAutoBoostedClocksEnabled)(nvmlDevice_t device, nvmlEnableState_t enabled, unsigned int flags);
    nvmlReturn_t (*nvmlDeviceSetDefaultFanSpeed_v2)(nvmlDevice_t device, unsigned int fan);
    nvmlReturn_t (*nvmlDeviceSetFanControlPolicy)(nvmlDevice_t device, unsigned int fan, nvmlFanControlPolicy_t policy);
    nvmlReturn_t (*nvmlDeviceSetTemperatureThreshold)(nvmlDevice_t device, nvmlTemperatureThresholds_t thresholdType, int * temp);
    nvmlReturn_t (*nvmlDeviceSetPowerManagementLimit)(nvmlDevice_t device, unsigned int limit);
    nvmlReturn_t (*nvmlDeviceSetGpuOperationMode)(nvmlDevice_t device, nvmlGpuOperationMode_t mode);
    nvmlReturn_t (*nvmlDeviceSetAPIRestriction)(nvmlDevice_t device, nvmlRestrictedAPI_t apiType, nvmlEnableState_t isRestricted);
    nvmlReturn_t (*nvmlDeviceSetFanSpeed_v2)(nvmlDevice_t device, unsigned int fan, unsigned int speed);
    nvmlReturn_t (*nvmlDeviceSetGpcClkVfOffset)(nvmlDevice_t device, int offset);
    nvmlReturn_t (*nvmlDeviceSetMemClkVfOffset)(nvmlDevice_t device, int offset);
    nvmlReturn_t (*nvmlDeviceSetAccountingMode)(nvmlDevice_t device, nvmlEnableState_t mode);
    nvmlReturn_t (*nvmlDeviceClearAccountingPids)(nvmlDevice_t device);
    nvmlReturn_t (*nvmlDeviceGetNvLinkState)(nvmlDevice_t device, unsigned int link, nvmlEnableState_t * isActive);
    nvmlReturn_t (*nvmlDeviceGetNvLinkVersion)(nvmlDevice_t device, unsigned int link, unsigned int * version);
    nvmlReturn_t (*nvmlDeviceGetNvLinkCapability)(nvmlDevice_t device, unsigned int link, nvmlNvLinkCapability_t capability, unsigned int * capResult);
    nvmlReturn_t (*nvmlDeviceGetNvLinkRemotePciInfo_v2)(nvmlDevice_t device, unsigned int link, nvmlPciInfo_t * pci);
    nvmlReturn_t (*nvmlDeviceGetNvLinkErrorCounter)(nvmlDevice_t device, unsigned int link, nvmlNvLinkErrorCounter_t counter, unsigned long long * counterValue);
    nvmlReturn_t (*nvmlDeviceResetNvLinkErrorCounters)(nvmlDevice_t device, unsigned int link);
    nvmlReturn_t (*nvmlDeviceSetNvLinkUtilizationControl)(nvmlDevice_t device, unsigned int link, unsigned int counter, nvmlNvLinkUtilizationControl_t * control, unsigned int reset);
    nvmlReturn_t (*nvmlDeviceGetNvLinkUtilizationControl)(nvmlDevice_t device, unsigned int link, unsigned int counter, nvmlNvLinkUtilizationControl_t * control);
    nvmlReturn_t (*nvmlDeviceGetNvLinkUtilizationCounter)(nvmlDevice_t device, unsigned int link, unsigned int counter, unsigned long long * rxcounter, unsigned long long * txcounter);
    nvmlReturn_t (*nvmlDeviceFreezeNvLinkUtilizationCounter)(nvmlDevice_t device, unsigned int link, unsigned int counter, nvmlEnableState_t freeze);
    nvmlReturn_t (*nvmlDeviceResetNvLinkUtilizationCounter)(nvmlDevice_t device, unsigned int link, unsigned int counter);
    nvmlReturn_t (*nvmlDeviceGetNvLinkRemoteDeviceType)(nvmlDevice_t device, unsigned int link, nvmlIntNvLinkDeviceType_t * pNvLinkDeviceType);
    nvmlReturn_t (*nvmlDeviceSetNvLinkDeviceLowPowerThreshold)(nvmlDevice_t device, nvmlNvLinkPowerThres_t * info);
    nvmlReturn_t (*nvmlSystemSetNvlinkBwMode)(unsigned int nvlinkBwMode);
    nvmlReturn_t (*nvmlSystemGetNvlinkBwMode)(unsigned int * nvlinkBwMode);
    nvmlReturn_t (*nvmlDeviceGetNvlinkSupportedBwModes)(nvmlDevice_t device, nvmlNvlinkSupportedBwModes_t * supportedBwMode);
    nvmlReturn_t (*nvmlDeviceGetNvlinkBwMode)(nvmlDevice_t device, nvmlNvlinkGetBwMode_t * getBwMode);
    nvmlReturn_t (*nvmlDeviceSetNvlinkBwMode)(nvmlDevice_t device, nvmlNvlinkSetBwMode_t * setBwMode);
    nvmlReturn_t (*nvmlDeviceGetNvLinkInfo)(nvmlDevice_t device, nvmlNvLinkInfo_t * info);
    nvmlReturn_t (*nvmlEventSetCreate)(nvmlEventSet_t * set);
    nvmlReturn_t (*nvmlDeviceRegisterEvents)(nvmlDevice_t device, unsigned long long eventTypes, nvmlEventSet_t set);
    nvmlReturn_t (*nvmlDeviceGetSupportedEventTypes)(nvmlDevice_t device, unsigned long long * eventTypes);
    nvmlReturn_t (*nvmlEventSetWait_v2)(nvmlEventSet_t set, nvmlEventData_t * data, unsigned int timeoutms);
    nvmlReturn_t (*nvmlEventSetFree)(nvmlEventSet_t set);
    nvmlReturn_t (*nvmlSystemEventSetCreate)(nvmlSystemEventSetCreateRequest_t * request);
    nvmlReturn_t (*nvmlSystemEventSetFree)(nvmlSystemEventSetFreeRequest_t * request);
    nvmlReturn_t (*nvmlSystemRegisterEvents)(nvmlSystemRegisterEventRequest_t * request);
    nvmlReturn_t (*nvmlSystemEventSetWait)(nvmlSystemEventSetWaitRequest_t * request);
    nvmlReturn_t (*nvmlDeviceModifyDrainState)(nvmlPciInfo_t * pciInfo, nvmlEnableState_t newState);
    nvmlReturn_t (*nvmlDeviceQueryDrainState)(nvmlPciInfo_t * pciInfo, nvmlEnableState_t * currentState);
    nvmlReturn_t (*nvmlDeviceRemoveGpu_v2)(nvmlPciInfo_t * pciInfo, nvmlDetachGpuState_t gpuState, nvmlPcieLinkState_t linkState);
    nvmlReturn_t (*nvmlDeviceDiscoverGpus)(nvmlPciInfo_t * pciInfo);
    nvmlReturn_t (*nvmlDeviceGetFieldValues)(nvmlDevice_t device, int valuesCount, nvmlFieldValue_t * values);
    nvmlReturn_t (*nvmlDeviceClearFieldValues)(nvmlDevice_t device, int valuesCount, nvmlFieldValue_t * values);
    nvmlReturn_t (*nvmlDeviceGetVirtualizationMode)(nvmlDevice_t device, nvmlGpuVirtualizationMode_t * pVirtualMode);
    nvmlReturn_t (*nvmlDeviceGetHostVgpuMode)(nvmlDevice_t device, nvmlHostVgpuMode_t * pHostVgpuMode);
    nvmlReturn_t (*nvmlDeviceSetVirtualizationMode)(nvmlDevice_t device, nvmlGpuVirtualizationMode_t virtualMode);
    nvmlReturn_t (*nvmlDeviceGetVgpuHeterogeneousMode)(nvmlDevice_t device, nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode);
    nvmlReturn_t (*nvmlDeviceSetVgpuHeterogeneousMode)(nvmlDevice_t device, const nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode);
    nvmlReturn_t (*nvmlVgpuInstanceGetPlacementId)(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuPlacementId_t * pPlacement);
    nvmlReturn_t (*nvmlDeviceGetVgpuTypeSupportedPlacements)(nvmlDevice_t device, nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuPlacementList_t * pPlacementList);
    nvmlReturn_t (*nvmlDeviceGetVgpuTypeCreatablePlacements)(nvmlDevice_t device, nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuPlacementList_t * pPlacementList);
    nvmlReturn_t (*nvmlVgpuTypeGetGspHeapSize)(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * gspHeapSize);
    nvmlReturn_t (*nvmlVgpuTypeGetFbReservation)(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * fbReservation);
    nvmlReturn_t (*nvmlVgpuInstanceGetRuntimeStateSize)(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuRuntimeState_t * pState);
    nvmlReturn_t (*nvmlDeviceSetVgpuCapabilities)(nvmlDevice_t device, nvmlDeviceVgpuCapability_t capability, nvmlEnableState_t state);
    nvmlReturn_t (*nvmlDeviceGetGridLicensableFeatures_v4)(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures);
    nvmlReturn_t (*nvmlGetVgpuDriverCapabilities)(nvmlVgpuDriverCapability_t capability, unsigned int * capResult);
    nvmlReturn_t (*nvmlDeviceGetVgpuCapabilities)(nvmlDevice_t device, nvmlDeviceVgpuCapability_t capability, unsigned int * capResult);
    nvmlReturn_t (*nvmlDeviceGetSupportedVgpus)(nvmlDevice_t device, unsigned int * vgpuCount, nvmlVgpuTypeId_t * vgpuTypeIds);
    nvmlReturn_t (*nvmlDeviceGetCreatableVgpus)(nvmlDevice_t device, unsigned int * vgpuCount, nvmlVgpuTypeId_t * vgpuTypeIds);
    nvmlReturn_t (*nvmlVgpuTypeGetClass)(nvmlVgpuTypeId_t vgpuTypeId, char * vgpuTypeClass, unsigned int * size);
    nvmlReturn_t (*nvmlVgpuTypeGetName)(nvmlVgpuTypeId_t vgpuTypeId, char * vgpuTypeName, unsigned int * size);
    nvmlReturn_t (*nvmlVgpuTypeGetGpuInstanceProfileId)(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * gpuInstanceProfileId);
    nvmlReturn_t (*nvmlVgpuTypeGetDeviceID)(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * deviceID, unsigned long long * subsystemID);
    nvmlReturn_t (*nvmlVgpuTypeGetFramebufferSize)(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * fbSize);
    nvmlReturn_t (*nvmlVgpuTypeGetNumDisplayHeads)(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * numDisplayHeads);
    nvmlReturn_t (*nvmlVgpuTypeGetResolution)(nvmlVgpuTypeId_t vgpuTypeId, unsigned int displayIndex, unsigned int * xdim, unsigned int * ydim);
    nvmlReturn_t (*nvmlVgpuTypeGetLicense)(nvmlVgpuTypeId_t vgpuTypeId, char * vgpuTypeLicenseString, unsigned int size);
    nvmlReturn_t (*nvmlVgpuTypeGetFrameRateLimit)(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * frameRateLimit);
    nvmlReturn_t (*nvmlVgpuTypeGetMaxInstances)(nvmlDevice_t device, nvmlVgpuTypeId_t vgpuTypeId, unsigned int * vgpuInstanceCount);
    nvmlReturn_t (*nvmlVgpuTypeGetMaxInstancesPerVm)(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * vgpuInstanceCountPerVm);
    nvmlReturn_t (*nvmlVgpuTypeGetBAR1Info)(nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuTypeBar1Info_t * bar1Info);
    nvmlReturn_t (*nvmlDeviceGetActiveVgpus)(nvmlDevice_t device, unsigned int * vgpuCount, nvmlVgpuInstance_t * vgpuInstances);
    nvmlReturn_t (*nvmlVgpuInstanceGetVmID)(nvmlVgpuInstance_t vgpuInstance, char * vmId, unsigned int size, nvmlVgpuVmIdType_t * vmIdType);
    nvmlReturn_t (*nvmlVgpuInstanceGetUUID)(nvmlVgpuInstance_t vgpuInstance, char * uuid, unsigned int size);
    nvmlReturn_t (*nvmlVgpuInstanceGetVmDriverVersion)(nvmlVgpuInstance_t vgpuInstance, char* version, unsigned int length);
    nvmlReturn_t (*nvmlVgpuInstanceGetFbUsage)(nvmlVgpuInstance_t vgpuInstance, unsigned long long * fbUsage);
    nvmlReturn_t (*nvmlVgpuInstanceGetLicenseStatus)(nvmlVgpuInstance_t vgpuInstance, unsigned int * licensed);
    nvmlReturn_t (*nvmlVgpuInstanceGetType)(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuTypeId_t * vgpuTypeId);
    nvmlReturn_t (*nvmlVgpuInstanceGetFrameRateLimit)(nvmlVgpuInstance_t vgpuInstance, unsigned int * frameRateLimit);
    nvmlReturn_t (*nvmlVgpuInstanceGetEccMode)(nvmlVgpuInstance_t vgpuInstance, nvmlEnableState_t * eccMode);
    nvmlReturn_t (*nvmlVgpuInstanceGetEncoderCapacity)(nvmlVgpuInstance_t vgpuInstance, unsigned int * encoderCapacity);
    nvmlReturn_t (*nvmlVgpuInstanceSetEncoderCapacity)(nvmlVgpuInstance_t vgpuInstance, unsigned int encoderCapacity);
    nvmlReturn_t (*nvmlVgpuInstanceGetEncoderStats)(nvmlVgpuInstance_t vgpuInstance, unsigned int * sessionCount, unsigned int * averageFps, unsigned int * averageLatency);
    nvmlReturn_t (*nvmlVgpuInstanceGetEncoderSessions)(nvmlVgpuInstance_t vgpuInstance, unsigned int * sessionCount, nvmlEncoderSessionInfo_t * sessionInfo);
    nvmlReturn_t (*nvmlVgpuInstanceGetFBCStats)(nvmlVgpuInstance_t vgpuInstance, nvmlFBCStats_t * fbcStats);
    nvmlReturn_t (*nvmlVgpuInstanceGetFBCSessions)(nvmlVgpuInstance_t vgpuInstance, unsigned int * sessionCount, nvmlFBCSessionInfo_t * sessionInfo);
    nvmlReturn_t (*nvmlVgpuInstanceGetGpuInstanceId)(nvmlVgpuInstance_t vgpuInstance, unsigned int * gpuInstanceId);
    nvmlReturn_t (*nvmlVgpuInstanceGetGpuPciId)(nvmlVgpuInstance_t vgpuInstance, char * vgpuPciId, unsigned int * length);
    nvmlReturn_t (*nvmlVgpuTypeGetCapabilities)(nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuCapability_t capability, unsigned int * capResult);
    nvmlReturn_t (*nvmlVgpuInstanceGetMdevUUID)(nvmlVgpuInstance_t vgpuInstance, char * mdevUuid, unsigned int size);
    nvmlReturn_t (*nvmlGpuInstanceGetCreatableVgpus)(nvmlGpuInstance_t gpuInstance, nvmlVgpuTypeIdInfo_t * pVgpus);
    nvmlReturn_t (*nvmlVgpuTypeGetMaxInstancesPerGpuInstance)(nvmlVgpuTypeMaxInstance_t * pMaxInstance);
    nvmlReturn_t (*nvmlGpuInstanceGetActiveVgpus)(nvmlGpuInstance_t gpuInstance, nvmlActiveVgpuInstanceInfo_t * pVgpuInstanceInfo);
    nvmlReturn_t (*nvmlGpuInstanceSetVgpuSchedulerState)(nvmlGpuInstance_t gpuInstance, nvmlVgpuSchedulerState_t * pScheduler);
    nvmlReturn_t (*nvmlGpuInstanceGetVgpuSchedulerState)(nvmlGpuInstance_t gpuInstance, nvmlVgpuSchedulerStateInfo_t * pSchedulerStateInfo);
    nvmlReturn_t (*nvmlGpuInstanceGetVgpuSchedulerLog)(nvmlGpuInstance_t gpuInstance, nvmlVgpuSchedulerLogInfo_t * pSchedulerLogInfo);
    nvmlReturn_t (*nvmlGpuInstanceGetVgpuTypeCreatablePlacements)(nvmlGpuInstance_t gpuInstance, nvmlVgpuCreatablePlacementInfo_t * pCreatablePlacementInfo);
    nvmlReturn_t (*nvmlGpuInstanceGetVgpuHeterogeneousMode)(nvmlGpuInstance_t gpuInstance, nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode);
    nvmlReturn_t (*nvmlGpuInstanceSetVgpuHeterogeneousMode)(nvmlGpuInstance_t gpuInstance, const nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode);
    nvmlReturn_t (*nvmlVgpuInstanceGetMetadata)(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuMetadata_t * vgpuMetadata, unsigned int * bufferSize);
    nvmlReturn_t (*nvmlDeviceGetVgpuMetadata)(nvmlDevice_t device, nvmlVgpuPgpuMetadata_t * pgpuMetadata, unsigned int * bufferSize);
    nvmlReturn_t (*nvmlGetVgpuCompatibility)(nvmlVgpuMetadata_t * vgpuMetadata, nvmlVgpuPgpuMetadata_t * pgpuMetadata, nvmlVgpuPgpuCompatibility_t * compatibilityInfo);
    nvmlReturn_t (*nvmlDeviceGetPgpuMetadataString)(nvmlDevice_t device, char * pgpuMetadata, unsigned int * bufferSize);
    nvmlReturn_t (*nvmlDeviceGetVgpuSchedulerLog)(nvmlDevice_t device, nvmlVgpuSchedulerLog_t * pSchedulerLog);
    nvmlReturn_t (*nvmlDeviceGetVgpuSchedulerState)(nvmlDevice_t device, nvmlVgpuSchedulerGetState_t * pSchedulerState);
    nvmlReturn_t (*nvmlDeviceGetVgpuSchedulerCapabilities)(nvmlDevice_t device, nvmlVgpuSchedulerCapabilities_t * pCapabilities);
    nvmlReturn_t (*nvmlDeviceSetVgpuSchedulerState)(nvmlDevice_t device, nvmlVgpuSchedulerSetState_t * pSchedulerState);
    nvmlReturn_t (*nvmlGetVgpuVersion)(nvmlVgpuVersion_t * supported, nvmlVgpuVersion_t * current);
    nvmlReturn_t (*nvmlSetVgpuVersion)(nvmlVgpuVersion_t * vgpuVersion);
    nvmlReturn_t (*nvmlDeviceGetVgpuUtilization)(nvmlDevice_t device, unsigned long long lastSeenTimeStamp, nvmlValueType_t * sampleValType, unsigned int * vgpuInstanceSamplesCount, nvmlVgpuInstanceUtilizationSample_t * utilizationSamples);
    nvmlReturn_t (*nvmlDeviceGetVgpuInstancesUtilizationInfo)(nvmlDevice_t device, nvmlVgpuInstancesUtilizationInfo_t * vgpuUtilInfo);
    nvmlReturn_t (*nvmlDeviceGetVgpuProcessUtilization)(nvmlDevice_t device, unsigned long long lastSeenTimeStamp, unsigned int * vgpuProcessSamplesCount, nvmlVgpuProcessUtilizationSample_t * utilizationSamples);
    nvmlReturn_t (*nvmlDeviceGetVgpuProcessesUtilizationInfo)(nvmlDevice_t device, nvmlVgpuProcessesUtilizationInfo_t * vgpuProcUtilInfo);
    nvmlReturn_t (*nvmlVgpuInstanceGetAccountingMode)(nvmlVgpuInstance_t vgpuInstance, nvmlEnableState_t * mode);
    nvmlReturn_t (*nvmlVgpuInstanceGetAccountingPids)(nvmlVgpuInstance_t vgpuInstance, unsigned int * count, unsigned int * pids);
    nvmlReturn_t (*nvmlVgpuInstanceGetAccountingStats)(nvmlVgpuInstance_t vgpuInstance, unsigned int pid, nvmlAccountingStats_t * stats);
    nvmlReturn_t (*nvmlVgpuInstanceClearAccountingPids)(nvmlVgpuInstance_t vgpuInstance);
    nvmlReturn_t (*nvmlVgpuInstanceGetLicenseInfo_v2)(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuLicenseInfo_t * licenseInfo);
    nvmlReturn_t (*nvmlGetExcludedDeviceCount)(unsigned int * deviceCount);
    nvmlReturn_t (*nvmlGetExcludedDeviceInfoByIndex)(unsigned int index, nvmlExcludedDeviceInfo_t * info);
    nvmlReturn_t (*nvmlDeviceReadWritePRM_v1)(nvmlDevice_t device, nvmlPRMTLV_v1_t * buffer);
    nvmlReturn_t (*nvmlDeviceSetMigMode)(nvmlDevice_t device, unsigned int mode, nvmlReturn_t * activationStatus);
    nvmlReturn_t (*nvmlDeviceGetMigMode)(nvmlDevice_t device, unsigned int * currentMode, unsigned int * pendingMode);
    nvmlReturn_t (*nvmlDeviceGetGpuInstanceProfileInfo)(nvmlDevice_t device, unsigned int profile, nvmlGpuInstanceProfileInfo_t * info);
    nvmlReturn_t (*nvmlDeviceGetGpuInstanceProfileInfoV)(nvmlDevice_t device, unsigned int profile, nvmlGpuInstanceProfileInfo_v2_t * info);
    nvmlReturn_t (*nvmlDeviceGetGpuInstanceProfileInfoByIdV)(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstanceProfileInfo_v2_t * info);
    nvmlReturn_t (*nvmlDeviceGetGpuInstancePossiblePlacements_v2)(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstancePlacement_t * placements, unsigned int * count);
    nvmlReturn_t (*nvmlDeviceGetGpuInstanceRemainingCapacity)(nvmlDevice_t device, unsigned int profileId, unsigned int * count);
    nvmlReturn_t (*nvmlDeviceCreateGpuInstance)(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstance_t * gpuInstance);
    nvmlReturn_t (*nvmlDeviceCreateGpuInstanceWithPlacement)(nvmlDevice_t device, unsigned int profileId, const nvmlGpuInstancePlacement_t * placement, nvmlGpuInstance_t * gpuInstance);
    nvmlReturn_t (*nvmlGpuInstanceDestroy)(nvmlGpuInstance_t gpuInstance);
    nvmlReturn_t (*nvmlDeviceGetGpuInstances)(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstance_t * gpuInstances, unsigned int * count);
    nvmlReturn_t (*nvmlDeviceGetGpuInstanceById)(nvmlDevice_t device, unsigned int id, nvmlGpuInstance_t * gpuInstance);
    nvmlReturn_t (*nvmlGpuInstanceGetInfo)(nvmlGpuInstance_t gpuInstance, nvmlGpuInstanceInfo_t * info);
    nvmlReturn_t (*nvmlGpuInstanceGetComputeInstanceProfileInfo)(nvmlGpuInstance_t gpuInstance, unsigned int profile, unsigned int engProfile, nvmlComputeInstanceProfileInfo_t * info);
    nvmlReturn_t (*nvmlGpuInstanceGetComputeInstanceProfileInfoV)(nvmlGpuInstance_t gpuInstance, unsigned int profile, unsigned int engProfile, nvmlComputeInstanceProfileInfo_v2_t * info);
    nvmlReturn_t (*nvmlGpuInstanceGetComputeInstanceRemainingCapacity)(nvmlGpuInstance_t gpuInstance, unsigned int profileId, unsigned int * count);
    nvmlReturn_t (*nvmlGpuInstanceGetComputeInstancePossiblePlacements)(nvmlGpuInstance_t gpuInstance, unsigned int profileId, nvmlComputeInstancePlacement_t * placements, unsigned int * count);
    nvmlReturn_t (*nvmlGpuInstanceCreateComputeInstance)(nvmlGpuInstance_t gpuInstance, unsigned int profileId, nvmlComputeInstance_t * computeInstance);
    nvmlReturn_t (*nvmlGpuInstanceCreateComputeInstanceWithPlacement)(nvmlGpuInstance_t gpuInstance, unsigned int profileId, const nvmlComputeInstancePlacement_t * placement, nvmlComputeInstance_t * computeInstance);
    nvmlReturn_t (*nvmlComputeInstanceDestroy)(nvmlComputeInstance_t computeInstance);
    nvmlReturn_t (*nvmlGpuInstanceGetComputeInstances)(nvmlGpuInstance_t gpuInstance, unsigned int profileId, nvmlComputeInstance_t * computeInstances, unsigned int * count);
    nvmlReturn_t (*nvmlGpuInstanceGetComputeInstanceById)(nvmlGpuInstance_t gpuInstance, unsigned int id, nvmlComputeInstance_t * computeInstance);
    nvmlReturn_t (*nvmlComputeInstanceGetInfo_v2)(nvmlComputeInstance_t computeInstance, nvmlComputeInstanceInfo_t * info);
    nvmlReturn_t (*nvmlDeviceIsMigDeviceHandle)(nvmlDevice_t device, unsigned int * isMigDevice);
    nvmlReturn_t (*nvmlDeviceGetGpuInstanceId)(nvmlDevice_t device, unsigned int * id);
    nvmlReturn_t (*nvmlDeviceGetComputeInstanceId)(nvmlDevice_t device, unsigned int * id);
    nvmlReturn_t (*nvmlDeviceGetMaxMigDeviceCount)(nvmlDevice_t device, unsigned int * count);
    nvmlReturn_t (*nvmlDeviceGetMigDeviceHandleByIndex)(nvmlDevice_t device, unsigned int index, nvmlDevice_t * migDevice);
    nvmlReturn_t (*nvmlDeviceGetDeviceHandleFromMigDeviceHandle)(nvmlDevice_t migDevice, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlGpmMetricsGet)(nvmlGpmMetricsGet_t * metricsGet);
    nvmlReturn_t (*nvmlGpmSampleFree)(nvmlGpmSample_t gpmSample);
    nvmlReturn_t (*nvmlGpmSampleAlloc)(nvmlGpmSample_t * gpmSample);
    nvmlReturn_t (*nvmlGpmSampleGet)(nvmlDevice_t device, nvmlGpmSample_t gpmSample);
    nvmlReturn_t (*nvmlGpmMigSampleGet)(nvmlDevice_t device, unsigned int gpuInstanceId, nvmlGpmSample_t gpmSample);
    nvmlReturn_t (*nvmlGpmQueryDeviceSupport)(nvmlDevice_t device, nvmlGpmSupport_t * gpmSupport);
    nvmlReturn_t (*nvmlGpmQueryIfStreamingEnabled)(nvmlDevice_t device, unsigned int * state);
    nvmlReturn_t (*nvmlGpmSetStreamingEnabled)(nvmlDevice_t device, unsigned int state);
    nvmlReturn_t (*nvmlDeviceGetCapabilities)(nvmlDevice_t device, nvmlDeviceCapabilities_t * caps);
    nvmlReturn_t (*nvmlDeviceWorkloadPowerProfileGetProfilesInfo)(nvmlDevice_t device, nvmlWorkloadPowerProfileProfilesInfo_t * profilesInfo);
    nvmlReturn_t (*nvmlDeviceWorkloadPowerProfileGetCurrentProfiles)(nvmlDevice_t device, nvmlWorkloadPowerProfileCurrentProfiles_t * currentProfiles);
    nvmlReturn_t (*nvmlDeviceWorkloadPowerProfileSetRequestedProfiles)(nvmlDevice_t device, nvmlWorkloadPowerProfileRequestedProfiles_t * requestedProfiles);
    nvmlReturn_t (*nvmlDeviceWorkloadPowerProfileClearRequestedProfiles)(nvmlDevice_t device, nvmlWorkloadPowerProfileRequestedProfiles_t * requestedProfiles);
    nvmlReturn_t (*nvmlDevicePowerSmoothingActivatePresetProfile)(nvmlDevice_t device, nvmlPowerSmoothingProfile_t * profile);
    nvmlReturn_t (*nvmlDevicePowerSmoothingUpdatePresetProfileParam)(nvmlDevice_t device, nvmlPowerSmoothingProfile_t * profile);
    nvmlReturn_t (*nvmlDevicePowerSmoothingSetState)(nvmlDevice_t device, nvmlPowerSmoothingState_t * state);
    nvmlReturn_t (*nvmlDeviceGetSramUniqueUncorrectedEccErrorCounts)(nvmlDevice_t device, nvmlEccSramUniqueUncorrectedErrorCounts_t * errorCounts);
    nvmlReturn_t (*nvmlInit)(void);
    nvmlReturn_t (*nvmlDeviceGetCount)(unsigned int * deviceCount);
    nvmlReturn_t (*nvmlDeviceGetHandleByIndex)(unsigned int index, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlDeviceGetHandleByPciBusId)(const char * pciBusId, nvmlDevice_t * device);
    nvmlReturn_t (*nvmlDeviceGetPciInfo)(nvmlDevice_t device, nvmlPciInfo_t * pci);
    nvmlReturn_t (*nvmlDeviceGetPciInfo_v2)(nvmlDevice_t device, nvmlPciInfo_t * pci);
    nvmlReturn_t (*nvmlDeviceGetNvLinkRemotePciInfo)(nvmlDevice_t device, unsigned int link, nvmlPciInfo_t * pci);
    nvmlReturn_t (*nvmlDeviceGetGridLicensableFeatures)(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures);
    nvmlReturn_t (*nvmlDeviceGetGridLicensableFeatures_v2)(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures);
    nvmlReturn_t (*nvmlDeviceGetGridLicensableFeatures_v3)(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures);
    nvmlReturn_t (*nvmlDeviceRemoveGpu)(nvmlPciInfo_t * pciInfo);
    nvmlReturn_t (*nvmlEventSetWait)(nvmlEventSet_t set, nvmlEventData_t * data, unsigned int timeoutms);
    nvmlReturn_t (*nvmlDeviceGetAttributes)(nvmlDevice_t device, nvmlDeviceAttributes_t * attributes);
    nvmlReturn_t (*nvmlComputeInstanceGetInfo)(nvmlComputeInstance_t computeInstance, nvmlComputeInstanceInfo_t * info);
    nvmlReturn_t (*nvmlDeviceGetComputeRunningProcesses)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v1_t * infos);
    nvmlReturn_t (*nvmlDeviceGetComputeRunningProcesses_v2)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v2_t * infos);
    nvmlReturn_t (*nvmlDeviceGetGraphicsRunningProcesses)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v1_t * infos);
    nvmlReturn_t (*nvmlDeviceGetGraphicsRunningProcesses_v2)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v2_t * infos);
    nvmlReturn_t (*nvmlDeviceGetMPSComputeRunningProcesses)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v1_t * infos);
    nvmlReturn_t (*nvmlDeviceGetMPSComputeRunningProcesses_v2)(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v2_t * infos);
    nvmlReturn_t (*nvmlDeviceGetGpuInstancePossiblePlacements)(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstancePlacement_t * placements, unsigned int * count);
    nvmlReturn_t (*nvmlVgpuInstanceGetLicenseInfo)(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuLicenseInfo_t * licenseInfo);
    nvmlReturn_t (*nvmlDeviceGetDriverModel)(nvmlDevice_t device, nvmlDriverModel_t * current, nvmlDriverModel_t * pending);

} nvml_dispatch_t;

extern nvml_dispatch_t nvml_dispatch;

// Static inline wrappers — cgo calls these instead of the NVML symbols directly,
// routing through the dispatch table rather than the PLT.
static inline nvmlReturn_t dispatch_nvmlInit_v2(void) {
    if (!nvml_dispatch.nvmlInit_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlInit_v2();
}

static inline nvmlReturn_t dispatch_nvmlInitWithFlags(unsigned int flags) {
    if (!nvml_dispatch.nvmlInitWithFlags) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlInitWithFlags(flags);
}

static inline nvmlReturn_t dispatch_nvmlShutdown(void) {
    if (!nvml_dispatch.nvmlShutdown) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlShutdown();
}

static inline const char* dispatch_nvmlErrorString(nvmlReturn_t result) {
    if (!nvml_dispatch.nvmlErrorString) return (const char*)0;
    return nvml_dispatch.nvmlErrorString(result);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetDriverVersion(char * version, unsigned int length) {
    if (!nvml_dispatch.nvmlSystemGetDriverVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetDriverVersion(version, length);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetNVMLVersion(char * version, unsigned int length) {
    if (!nvml_dispatch.nvmlSystemGetNVMLVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetNVMLVersion(version, length);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetCudaDriverVersion(int * cudaDriverVersion) {
    if (!nvml_dispatch.nvmlSystemGetCudaDriverVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetCudaDriverVersion(cudaDriverVersion);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetCudaDriverVersion_v2(int * cudaDriverVersion) {
    if (!nvml_dispatch.nvmlSystemGetCudaDriverVersion_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetCudaDriverVersion_v2(cudaDriverVersion);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetProcessName(unsigned int pid, char * name, unsigned int length) {
    if (!nvml_dispatch.nvmlSystemGetProcessName) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetProcessName(pid, name, length);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetHicVersion(unsigned int * hwbcCount, nvmlHwbcEntry_t * hwbcEntries) {
    if (!nvml_dispatch.nvmlSystemGetHicVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetHicVersion(hwbcCount, hwbcEntries);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetTopologyGpuSet(unsigned int cpuNumber, unsigned int * count, nvmlDevice_t * deviceArray) {
    if (!nvml_dispatch.nvmlSystemGetTopologyGpuSet) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetTopologyGpuSet(cpuNumber, count, deviceArray);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetDriverBranch(nvmlSystemDriverBranchInfo_t * branchInfo, unsigned int length) {
    if (!nvml_dispatch.nvmlSystemGetDriverBranch) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetDriverBranch(branchInfo, length);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetCount(unsigned int * unitCount) {
    if (!nvml_dispatch.nvmlUnitGetCount) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetCount(unitCount);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetHandleByIndex(unsigned int index, nvmlUnit_t * unit) {
    if (!nvml_dispatch.nvmlUnitGetHandleByIndex) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetHandleByIndex(index, unit);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetUnitInfo(nvmlUnit_t unit, nvmlUnitInfo_t * info) {
    if (!nvml_dispatch.nvmlUnitGetUnitInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetUnitInfo(unit, info);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetLedState(nvmlUnit_t unit, nvmlLedState_t * state) {
    if (!nvml_dispatch.nvmlUnitGetLedState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetLedState(unit, state);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetPsuInfo(nvmlUnit_t unit, nvmlPSUInfo_t * psu) {
    if (!nvml_dispatch.nvmlUnitGetPsuInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetPsuInfo(unit, psu);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetTemperature(nvmlUnit_t unit, unsigned int type, unsigned int * temp) {
    if (!nvml_dispatch.nvmlUnitGetTemperature) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetTemperature(unit, type, temp);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetFanSpeedInfo(nvmlUnit_t unit, nvmlUnitFanSpeeds_t * fanSpeeds) {
    if (!nvml_dispatch.nvmlUnitGetFanSpeedInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetFanSpeedInfo(unit, fanSpeeds);
}

static inline nvmlReturn_t dispatch_nvmlUnitGetDevices(nvmlUnit_t unit, unsigned int * deviceCount, nvmlDevice_t * devices) {
    if (!nvml_dispatch.nvmlUnitGetDevices) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitGetDevices(unit, deviceCount, devices);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCount_v2(unsigned int * deviceCount) {
    if (!nvml_dispatch.nvmlDeviceGetCount_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCount_v2(deviceCount);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAttributes_v2(nvmlDevice_t device, nvmlDeviceAttributes_t * attributes) {
    if (!nvml_dispatch.nvmlDeviceGetAttributes_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAttributes_v2(device, attributes);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHandleByIndex_v2(unsigned int index, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetHandleByIndex_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHandleByIndex_v2(index, device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHandleBySerial(const char * serial, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetHandleBySerial) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHandleBySerial(serial, device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHandleByUUID(const char * uuid, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetHandleByUUID) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHandleByUUID(uuid, device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHandleByUUIDV(const nvmlUUID_t * uuid, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetHandleByUUIDV) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHandleByUUIDV(uuid, device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHandleByPciBusId_v2(const char * pciBusId, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetHandleByPciBusId_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHandleByPciBusId_v2(pciBusId, device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetName(nvmlDevice_t device, char * name, unsigned int length) {
    if (!nvml_dispatch.nvmlDeviceGetName) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetName(device, name, length);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetBrand(nvmlDevice_t device, nvmlBrandType_t * type) {
    if (!nvml_dispatch.nvmlDeviceGetBrand) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetBrand(device, type);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetIndex(nvmlDevice_t device, unsigned int * index) {
    if (!nvml_dispatch.nvmlDeviceGetIndex) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetIndex(device, index);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSerial(nvmlDevice_t device, char * serial, unsigned int length) {
    if (!nvml_dispatch.nvmlDeviceGetSerial) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSerial(device, serial, length);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetModuleId(nvmlDevice_t device, unsigned int * moduleId) {
    if (!nvml_dispatch.nvmlDeviceGetModuleId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetModuleId(device, moduleId);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetC2cModeInfoV(nvmlDevice_t device, nvmlC2cModeInfo_v1_t * c2cModeInfo) {
    if (!nvml_dispatch.nvmlDeviceGetC2cModeInfoV) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetC2cModeInfoV(device, c2cModeInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMemoryAffinity(nvmlDevice_t device, unsigned int nodeSetSize, unsigned long * nodeSet, nvmlAffinityScope_t scope) {
    if (!nvml_dispatch.nvmlDeviceGetMemoryAffinity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMemoryAffinity(device, nodeSetSize, nodeSet, scope);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCpuAffinityWithinScope(nvmlDevice_t device, unsigned int cpuSetSize, unsigned long * cpuSet, nvmlAffinityScope_t scope) {
    if (!nvml_dispatch.nvmlDeviceGetCpuAffinityWithinScope) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCpuAffinityWithinScope(device, cpuSetSize, cpuSet, scope);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCpuAffinity(nvmlDevice_t device, unsigned int cpuSetSize, unsigned long * cpuSet) {
    if (!nvml_dispatch.nvmlDeviceGetCpuAffinity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCpuAffinity(device, cpuSetSize, cpuSet);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetCpuAffinity(nvmlDevice_t device) {
    if (!nvml_dispatch.nvmlDeviceSetCpuAffinity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetCpuAffinity(device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceClearCpuAffinity(nvmlDevice_t device) {
    if (!nvml_dispatch.nvmlDeviceClearCpuAffinity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceClearCpuAffinity(device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNumaNodeId(nvmlDevice_t device, unsigned int * node) {
    if (!nvml_dispatch.nvmlDeviceGetNumaNodeId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNumaNodeId(device, node);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAddressingMode(nvmlDevice_t device, nvmlDeviceAddressingMode_t * mode) {
    if (!nvml_dispatch.nvmlDeviceGetAddressingMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAddressingMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetRepairStatus(nvmlDevice_t device, nvmlRepairStatus_t * repairStatus) {
    if (!nvml_dispatch.nvmlDeviceGetRepairStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetRepairStatus(device, repairStatus);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTopologyCommonAncestor(nvmlDevice_t device1, nvmlDevice_t device2, nvmlGpuTopologyLevel_t * pathInfo) {
    if (!nvml_dispatch.nvmlDeviceGetTopologyCommonAncestor) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTopologyCommonAncestor(device1, device2, pathInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTopologyNearestGpus(nvmlDevice_t device, nvmlGpuTopologyLevel_t level, unsigned int * count, nvmlDevice_t * deviceArray) {
    if (!nvml_dispatch.nvmlDeviceGetTopologyNearestGpus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTopologyNearestGpus(device, level, count, deviceArray);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetP2PStatus(nvmlDevice_t device1, nvmlDevice_t device2, nvmlGpuP2PCapsIndex_t p2pIndex, nvmlGpuP2PStatus_t * p2pStatus) {
    if (!nvml_dispatch.nvmlDeviceGetP2PStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetP2PStatus(device1, device2, p2pIndex, p2pStatus);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetUUID(nvmlDevice_t device, char * uuid, unsigned int length) {
    if (!nvml_dispatch.nvmlDeviceGetUUID) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetUUID(device, uuid, length);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMinorNumber(nvmlDevice_t device, unsigned int * minorNumber) {
    if (!nvml_dispatch.nvmlDeviceGetMinorNumber) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMinorNumber(device, minorNumber);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetBoardPartNumber(nvmlDevice_t device, char* partNumber, unsigned int length) {
    if (!nvml_dispatch.nvmlDeviceGetBoardPartNumber) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetBoardPartNumber(device, partNumber, length);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetInforomVersion(nvmlDevice_t device, nvmlInforomObject_t object, char * version, unsigned int length) {
    if (!nvml_dispatch.nvmlDeviceGetInforomVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetInforomVersion(device, object, version, length);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetInforomImageVersion(nvmlDevice_t device, char * version, unsigned int length) {
    if (!nvml_dispatch.nvmlDeviceGetInforomImageVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetInforomImageVersion(device, version, length);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetInforomConfigurationChecksum(nvmlDevice_t device, unsigned int * checksum) {
    if (!nvml_dispatch.nvmlDeviceGetInforomConfigurationChecksum) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetInforomConfigurationChecksum(device, checksum);
}

static inline nvmlReturn_t dispatch_nvmlDeviceValidateInforom(nvmlDevice_t device) {
    if (!nvml_dispatch.nvmlDeviceValidateInforom) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceValidateInforom(device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetLastBBXFlushTime(nvmlDevice_t device, unsigned long long * timestamp, unsigned long * durationUs) {
    if (!nvml_dispatch.nvmlDeviceGetLastBBXFlushTime) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetLastBBXFlushTime(device, timestamp, durationUs);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDisplayMode(nvmlDevice_t device, nvmlEnableState_t * display) {
    if (!nvml_dispatch.nvmlDeviceGetDisplayMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDisplayMode(device, display);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDisplayActive(nvmlDevice_t device, nvmlEnableState_t * isActive) {
    if (!nvml_dispatch.nvmlDeviceGetDisplayActive) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDisplayActive(device, isActive);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPersistenceMode(nvmlDevice_t device, nvmlEnableState_t * mode) {
    if (!nvml_dispatch.nvmlDeviceGetPersistenceMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPersistenceMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPciInfoExt(nvmlDevice_t device, nvmlPciInfoExt_t * pci) {
    if (!nvml_dispatch.nvmlDeviceGetPciInfoExt) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPciInfoExt(device, pci);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPciInfo_v3(nvmlDevice_t device, nvmlPciInfo_t * pci) {
    if (!nvml_dispatch.nvmlDeviceGetPciInfo_v3) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPciInfo_v3(device, pci);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMaxPcieLinkGeneration(nvmlDevice_t device, unsigned int * maxLinkGen) {
    if (!nvml_dispatch.nvmlDeviceGetMaxPcieLinkGeneration) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMaxPcieLinkGeneration(device, maxLinkGen);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuMaxPcieLinkGeneration(nvmlDevice_t device, unsigned int * maxLinkGenDevice) {
    if (!nvml_dispatch.nvmlDeviceGetGpuMaxPcieLinkGeneration) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuMaxPcieLinkGeneration(device, maxLinkGenDevice);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMaxPcieLinkWidth(nvmlDevice_t device, unsigned int * maxLinkWidth) {
    if (!nvml_dispatch.nvmlDeviceGetMaxPcieLinkWidth) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMaxPcieLinkWidth(device, maxLinkWidth);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCurrPcieLinkGeneration(nvmlDevice_t device, unsigned int * currLinkGen) {
    if (!nvml_dispatch.nvmlDeviceGetCurrPcieLinkGeneration) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCurrPcieLinkGeneration(device, currLinkGen);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCurrPcieLinkWidth(nvmlDevice_t device, unsigned int * currLinkWidth) {
    if (!nvml_dispatch.nvmlDeviceGetCurrPcieLinkWidth) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCurrPcieLinkWidth(device, currLinkWidth);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPcieThroughput(nvmlDevice_t device, nvmlPcieUtilCounter_t counter, unsigned int * value) {
    if (!nvml_dispatch.nvmlDeviceGetPcieThroughput) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPcieThroughput(device, counter, value);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPcieReplayCounter(nvmlDevice_t device, unsigned int * value) {
    if (!nvml_dispatch.nvmlDeviceGetPcieReplayCounter) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPcieReplayCounter(device, value);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetClockInfo(nvmlDevice_t device, nvmlClockType_t type, unsigned int * clock) {
    if (!nvml_dispatch.nvmlDeviceGetClockInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetClockInfo(device, type, clock);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMaxClockInfo(nvmlDevice_t device, nvmlClockType_t type, unsigned int * clock) {
    if (!nvml_dispatch.nvmlDeviceGetMaxClockInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMaxClockInfo(device, type, clock);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpcClkVfOffset(nvmlDevice_t device, int * offset) {
    if (!nvml_dispatch.nvmlDeviceGetGpcClkVfOffset) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpcClkVfOffset(device, offset);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetApplicationsClock(nvmlDevice_t device, nvmlClockType_t clockType, unsigned int * clockMHz) {
    if (!nvml_dispatch.nvmlDeviceGetApplicationsClock) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetApplicationsClock(device, clockType, clockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDefaultApplicationsClock(nvmlDevice_t device, nvmlClockType_t clockType, unsigned int * clockMHz) {
    if (!nvml_dispatch.nvmlDeviceGetDefaultApplicationsClock) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDefaultApplicationsClock(device, clockType, clockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetClock(nvmlDevice_t device, nvmlClockType_t clockType, nvmlClockId_t clockId, unsigned int * clockMHz) {
    if (!nvml_dispatch.nvmlDeviceGetClock) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetClock(device, clockType, clockId, clockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMaxCustomerBoostClock(nvmlDevice_t device, nvmlClockType_t clockType, unsigned int * clockMHz) {
    if (!nvml_dispatch.nvmlDeviceGetMaxCustomerBoostClock) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMaxCustomerBoostClock(device, clockType, clockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSupportedMemoryClocks(nvmlDevice_t device, unsigned int * count, unsigned int * clocksMHz) {
    if (!nvml_dispatch.nvmlDeviceGetSupportedMemoryClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSupportedMemoryClocks(device, count, clocksMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSupportedGraphicsClocks(nvmlDevice_t device, unsigned int memoryClockMHz, unsigned int * count, unsigned int * clocksMHz) {
    if (!nvml_dispatch.nvmlDeviceGetSupportedGraphicsClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSupportedGraphicsClocks(device, memoryClockMHz, count, clocksMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAutoBoostedClocksEnabled(nvmlDevice_t device, nvmlEnableState_t * isEnabled, nvmlEnableState_t * defaultIsEnabled) {
    if (!nvml_dispatch.nvmlDeviceGetAutoBoostedClocksEnabled) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAutoBoostedClocksEnabled(device, isEnabled, defaultIsEnabled);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetFanSpeed(nvmlDevice_t device, unsigned int * speed) {
    if (!nvml_dispatch.nvmlDeviceGetFanSpeed) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetFanSpeed(device, speed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetFanSpeed_v2(nvmlDevice_t device, unsigned int fan, unsigned int * speed) {
    if (!nvml_dispatch.nvmlDeviceGetFanSpeed_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetFanSpeed_v2(device, fan, speed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetFanSpeedRPM(nvmlDevice_t device, nvmlFanSpeedInfo_t * fanSpeed) {
    if (!nvml_dispatch.nvmlDeviceGetFanSpeedRPM) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetFanSpeedRPM(device, fanSpeed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTargetFanSpeed(nvmlDevice_t device, unsigned int fan, unsigned int * targetSpeed) {
    if (!nvml_dispatch.nvmlDeviceGetTargetFanSpeed) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTargetFanSpeed(device, fan, targetSpeed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMinMaxFanSpeed(nvmlDevice_t device, unsigned int * minSpeed, unsigned int * maxSpeed) {
    if (!nvml_dispatch.nvmlDeviceGetMinMaxFanSpeed) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMinMaxFanSpeed(device, minSpeed, maxSpeed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetFanControlPolicy_v2(nvmlDevice_t device, unsigned int fan, nvmlFanControlPolicy_t * policy) {
    if (!nvml_dispatch.nvmlDeviceGetFanControlPolicy_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetFanControlPolicy_v2(device, fan, policy);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNumFans(nvmlDevice_t device, unsigned int * numFans) {
    if (!nvml_dispatch.nvmlDeviceGetNumFans) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNumFans(device, numFans);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTemperature(nvmlDevice_t device, nvmlTemperatureSensors_t sensorType, unsigned int * temp) {
    if (!nvml_dispatch.nvmlDeviceGetTemperature) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTemperature(device, sensorType, temp);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCoolerInfo(nvmlDevice_t device, nvmlCoolerInfo_t * coolerInfo) {
    if (!nvml_dispatch.nvmlDeviceGetCoolerInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCoolerInfo(device, coolerInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTemperatureV(nvmlDevice_t device, nvmlTemperature_t * temperature) {
    if (!nvml_dispatch.nvmlDeviceGetTemperatureV) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTemperatureV(device, temperature);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTemperatureThreshold(nvmlDevice_t device, nvmlTemperatureThresholds_t thresholdType, unsigned int * temp) {
    if (!nvml_dispatch.nvmlDeviceGetTemperatureThreshold) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTemperatureThreshold(device, thresholdType, temp);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMarginTemperature(nvmlDevice_t device, nvmlMarginTemperature_t * marginTempInfo) {
    if (!nvml_dispatch.nvmlDeviceGetMarginTemperature) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMarginTemperature(device, marginTempInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetThermalSettings(nvmlDevice_t device, unsigned int sensorIndex, nvmlGpuThermalSettings_t * pThermalSettings) {
    if (!nvml_dispatch.nvmlDeviceGetThermalSettings) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetThermalSettings(device, sensorIndex, pThermalSettings);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPerformanceState(nvmlDevice_t device, nvmlPstates_t * pState) {
    if (!nvml_dispatch.nvmlDeviceGetPerformanceState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPerformanceState(device, pState);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCurrentClocksEventReasons(nvmlDevice_t device, unsigned long long * clocksEventReasons) {
    if (!nvml_dispatch.nvmlDeviceGetCurrentClocksEventReasons) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCurrentClocksEventReasons(device, clocksEventReasons);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCurrentClocksThrottleReasons(nvmlDevice_t device, unsigned long long * clocksThrottleReasons) {
    if (!nvml_dispatch.nvmlDeviceGetCurrentClocksThrottleReasons) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCurrentClocksThrottleReasons(device, clocksThrottleReasons);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSupportedClocksEventReasons(nvmlDevice_t device, unsigned long long * supportedClocksEventReasons) {
    if (!nvml_dispatch.nvmlDeviceGetSupportedClocksEventReasons) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSupportedClocksEventReasons(device, supportedClocksEventReasons);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSupportedClocksThrottleReasons(nvmlDevice_t device, unsigned long long * supportedClocksThrottleReasons) {
    if (!nvml_dispatch.nvmlDeviceGetSupportedClocksThrottleReasons) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSupportedClocksThrottleReasons(device, supportedClocksThrottleReasons);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerState(nvmlDevice_t device, nvmlPstates_t * pState) {
    if (!nvml_dispatch.nvmlDeviceGetPowerState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerState(device, pState);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDynamicPstatesInfo(nvmlDevice_t device, nvmlGpuDynamicPstatesInfo_t * pDynamicPstatesInfo) {
    if (!nvml_dispatch.nvmlDeviceGetDynamicPstatesInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDynamicPstatesInfo(device, pDynamicPstatesInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMemClkVfOffset(nvmlDevice_t device, int * offset) {
    if (!nvml_dispatch.nvmlDeviceGetMemClkVfOffset) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMemClkVfOffset(device, offset);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMinMaxClockOfPState(nvmlDevice_t device, nvmlClockType_t type, nvmlPstates_t pstate, unsigned int * minClockMHz, unsigned int * maxClockMHz) {
    if (!nvml_dispatch.nvmlDeviceGetMinMaxClockOfPState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMinMaxClockOfPState(device, type, pstate, minClockMHz, maxClockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSupportedPerformanceStates(nvmlDevice_t device, nvmlPstates_t * pstates, unsigned int size) {
    if (!nvml_dispatch.nvmlDeviceGetSupportedPerformanceStates) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSupportedPerformanceStates(device, pstates, size);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpcClkMinMaxVfOffset(nvmlDevice_t device, int * minOffset, int * maxOffset) {
    if (!nvml_dispatch.nvmlDeviceGetGpcClkMinMaxVfOffset) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpcClkMinMaxVfOffset(device, minOffset, maxOffset);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMemClkMinMaxVfOffset(nvmlDevice_t device, int * minOffset, int * maxOffset) {
    if (!nvml_dispatch.nvmlDeviceGetMemClkMinMaxVfOffset) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMemClkMinMaxVfOffset(device, minOffset, maxOffset);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetClockOffsets(nvmlDevice_t device, nvmlClockOffset_t * info) {
    if (!nvml_dispatch.nvmlDeviceGetClockOffsets) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetClockOffsets(device, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetClockOffsets(nvmlDevice_t device, nvmlClockOffset_t * info) {
    if (!nvml_dispatch.nvmlDeviceSetClockOffsets) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetClockOffsets(device, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPerformanceModes(nvmlDevice_t device, nvmlDevicePerfModes_t * perfModes) {
    if (!nvml_dispatch.nvmlDeviceGetPerformanceModes) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPerformanceModes(device, perfModes);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCurrentClockFreqs(nvmlDevice_t device, nvmlDeviceCurrentClockFreqs_t * currentClockFreqs) {
    if (!nvml_dispatch.nvmlDeviceGetCurrentClockFreqs) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCurrentClockFreqs(device, currentClockFreqs);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerManagementMode(nvmlDevice_t device, nvmlEnableState_t * mode) {
    if (!nvml_dispatch.nvmlDeviceGetPowerManagementMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerManagementMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerManagementLimit(nvmlDevice_t device, unsigned int * limit) {
    if (!nvml_dispatch.nvmlDeviceGetPowerManagementLimit) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerManagementLimit(device, limit);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerManagementLimitConstraints(nvmlDevice_t device, unsigned int * minLimit, unsigned int * maxLimit) {
    if (!nvml_dispatch.nvmlDeviceGetPowerManagementLimitConstraints) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerManagementLimitConstraints(device, minLimit, maxLimit);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerManagementDefaultLimit(nvmlDevice_t device, unsigned int * defaultLimit) {
    if (!nvml_dispatch.nvmlDeviceGetPowerManagementDefaultLimit) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerManagementDefaultLimit(device, defaultLimit);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerUsage(nvmlDevice_t device, unsigned int * power) {
    if (!nvml_dispatch.nvmlDeviceGetPowerUsage) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerUsage(device, power);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerMizerMode_v1(nvmlDevice_t device, nvmlDevicePowerMizerModes_v1_t * powerMizerMode) {
    if (!nvml_dispatch.nvmlDeviceGetPowerMizerMode_v1) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerMizerMode_v1(device, powerMizerMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetPowerMizerMode_v1(nvmlDevice_t device, nvmlDevicePowerMizerModes_v1_t * powerMizerMode) {
    if (!nvml_dispatch.nvmlDeviceSetPowerMizerMode_v1) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetPowerMizerMode_v1(device, powerMizerMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTotalEnergyConsumption(nvmlDevice_t device, unsigned long long * energy) {
    if (!nvml_dispatch.nvmlDeviceGetTotalEnergyConsumption) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTotalEnergyConsumption(device, energy);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetEnforcedPowerLimit(nvmlDevice_t device, unsigned int * limit) {
    if (!nvml_dispatch.nvmlDeviceGetEnforcedPowerLimit) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetEnforcedPowerLimit(device, limit);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuOperationMode(nvmlDevice_t device, nvmlGpuOperationMode_t * current, nvmlGpuOperationMode_t * pending) {
    if (!nvml_dispatch.nvmlDeviceGetGpuOperationMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuOperationMode(device, current, pending);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMemoryInfo(nvmlDevice_t device, nvmlMemory_t * memory) {
    if (!nvml_dispatch.nvmlDeviceGetMemoryInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMemoryInfo(device, memory);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMemoryInfo_v2(nvmlDevice_t device, nvmlMemory_v2_t * memory) {
    if (!nvml_dispatch.nvmlDeviceGetMemoryInfo_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMemoryInfo_v2(device, memory);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetComputeMode(nvmlDevice_t device, nvmlComputeMode_t * mode) {
    if (!nvml_dispatch.nvmlDeviceGetComputeMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetComputeMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCudaComputeCapability(nvmlDevice_t device, int * major, int * minor) {
    if (!nvml_dispatch.nvmlDeviceGetCudaComputeCapability) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCudaComputeCapability(device, major, minor);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDramEncryptionMode(nvmlDevice_t device, nvmlDramEncryptionInfo_t * current, nvmlDramEncryptionInfo_t * pending) {
    if (!nvml_dispatch.nvmlDeviceGetDramEncryptionMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDramEncryptionMode(device, current, pending);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetDramEncryptionMode(nvmlDevice_t device, const nvmlDramEncryptionInfo_t * dramEncryption) {
    if (!nvml_dispatch.nvmlDeviceSetDramEncryptionMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetDramEncryptionMode(device, dramEncryption);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetEccMode(nvmlDevice_t device, nvmlEnableState_t * current, nvmlEnableState_t * pending) {
    if (!nvml_dispatch.nvmlDeviceGetEccMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetEccMode(device, current, pending);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDefaultEccMode(nvmlDevice_t device, nvmlEnableState_t * defaultMode) {
    if (!nvml_dispatch.nvmlDeviceGetDefaultEccMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDefaultEccMode(device, defaultMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetBoardId(nvmlDevice_t device, unsigned int * boardId) {
    if (!nvml_dispatch.nvmlDeviceGetBoardId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetBoardId(device, boardId);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMultiGpuBoard(nvmlDevice_t device, unsigned int * multiGpuBool) {
    if (!nvml_dispatch.nvmlDeviceGetMultiGpuBoard) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMultiGpuBoard(device, multiGpuBool);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetTotalEccErrors(nvmlDevice_t device, nvmlMemoryErrorType_t errorType, nvmlEccCounterType_t counterType, unsigned long long * eccCounts) {
    if (!nvml_dispatch.nvmlDeviceGetTotalEccErrors) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetTotalEccErrors(device, errorType, counterType, eccCounts);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDetailedEccErrors(nvmlDevice_t device, nvmlMemoryErrorType_t errorType, nvmlEccCounterType_t counterType, nvmlEccErrorCounts_t * eccCounts) {
    if (!nvml_dispatch.nvmlDeviceGetDetailedEccErrors) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDetailedEccErrors(device, errorType, counterType, eccCounts);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMemoryErrorCounter(nvmlDevice_t device, nvmlMemoryErrorType_t errorType, nvmlEccCounterType_t counterType, nvmlMemoryLocation_t locationType, unsigned long long * count) {
    if (!nvml_dispatch.nvmlDeviceGetMemoryErrorCounter) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMemoryErrorCounter(device, errorType, counterType, locationType, count);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetUtilizationRates(nvmlDevice_t device, nvmlUtilization_t * utilization) {
    if (!nvml_dispatch.nvmlDeviceGetUtilizationRates) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetUtilizationRates(device, utilization);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetEncoderUtilization(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs) {
    if (!nvml_dispatch.nvmlDeviceGetEncoderUtilization) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetEncoderUtilization(device, utilization, samplingPeriodUs);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetEncoderCapacity(nvmlDevice_t device, nvmlEncoderType_t encoderQueryType, unsigned int * encoderCapacity) {
    if (!nvml_dispatch.nvmlDeviceGetEncoderCapacity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetEncoderCapacity(device, encoderQueryType, encoderCapacity);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetEncoderStats(nvmlDevice_t device, unsigned int * sessionCount, unsigned int * averageFps, unsigned int * averageLatency) {
    if (!nvml_dispatch.nvmlDeviceGetEncoderStats) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetEncoderStats(device, sessionCount, averageFps, averageLatency);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetEncoderSessions(nvmlDevice_t device, unsigned int * sessionCount, nvmlEncoderSessionInfo_t * sessionInfos) {
    if (!nvml_dispatch.nvmlDeviceGetEncoderSessions) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetEncoderSessions(device, sessionCount, sessionInfos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDecoderUtilization(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs) {
    if (!nvml_dispatch.nvmlDeviceGetDecoderUtilization) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDecoderUtilization(device, utilization, samplingPeriodUs);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetJpgUtilization(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs) {
    if (!nvml_dispatch.nvmlDeviceGetJpgUtilization) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetJpgUtilization(device, utilization, samplingPeriodUs);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetOfaUtilization(nvmlDevice_t device, unsigned int * utilization, unsigned int * samplingPeriodUs) {
    if (!nvml_dispatch.nvmlDeviceGetOfaUtilization) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetOfaUtilization(device, utilization, samplingPeriodUs);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetFBCStats(nvmlDevice_t device, nvmlFBCStats_t * fbcStats) {
    if (!nvml_dispatch.nvmlDeviceGetFBCStats) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetFBCStats(device, fbcStats);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetFBCSessions(nvmlDevice_t device, unsigned int * sessionCount, nvmlFBCSessionInfo_t * sessionInfo) {
    if (!nvml_dispatch.nvmlDeviceGetFBCSessions) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetFBCSessions(device, sessionCount, sessionInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDriverModel_v2(nvmlDevice_t device, nvmlDriverModel_t * current, nvmlDriverModel_t * pending) {
    if (!nvml_dispatch.nvmlDeviceGetDriverModel_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDriverModel_v2(device, current, pending);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVbiosVersion(nvmlDevice_t device, char * version, unsigned int length) {
    if (!nvml_dispatch.nvmlDeviceGetVbiosVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVbiosVersion(device, version, length);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetBridgeChipInfo(nvmlDevice_t device, nvmlBridgeChipHierarchy_t * bridgeHierarchy) {
    if (!nvml_dispatch.nvmlDeviceGetBridgeChipInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetBridgeChipInfo(device, bridgeHierarchy);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetComputeRunningProcesses_v3(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetComputeRunningProcesses_v3) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetComputeRunningProcesses_v3(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGraphicsRunningProcesses_v3(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetGraphicsRunningProcesses_v3) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGraphicsRunningProcesses_v3(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMPSComputeRunningProcesses_v3(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetMPSComputeRunningProcesses_v3) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMPSComputeRunningProcesses_v3(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetRunningProcessDetailList(nvmlDevice_t device, nvmlProcessDetailList_t * plist) {
    if (!nvml_dispatch.nvmlDeviceGetRunningProcessDetailList) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetRunningProcessDetailList(device, plist);
}

static inline nvmlReturn_t dispatch_nvmlDeviceOnSameBoard(nvmlDevice_t device1, nvmlDevice_t device2, int * onSameBoard) {
    if (!nvml_dispatch.nvmlDeviceOnSameBoard) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceOnSameBoard(device1, device2, onSameBoard);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAPIRestriction(nvmlDevice_t device, nvmlRestrictedAPI_t apiType, nvmlEnableState_t * isRestricted) {
    if (!nvml_dispatch.nvmlDeviceGetAPIRestriction) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAPIRestriction(device, apiType, isRestricted);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSamples(nvmlDevice_t device, nvmlSamplingType_t type, unsigned long long lastSeenTimeStamp, nvmlValueType_t * sampleValType, unsigned int * sampleCount, nvmlSample_t * samples) {
    if (!nvml_dispatch.nvmlDeviceGetSamples) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSamples(device, type, lastSeenTimeStamp, sampleValType, sampleCount, samples);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetBAR1MemoryInfo(nvmlDevice_t device, nvmlBAR1Memory_t * bar1Memory) {
    if (!nvml_dispatch.nvmlDeviceGetBAR1MemoryInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetBAR1MemoryInfo(device, bar1Memory);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetViolationStatus(nvmlDevice_t device, nvmlPerfPolicyType_t perfPolicyType, nvmlViolationTime_t * violTime) {
    if (!nvml_dispatch.nvmlDeviceGetViolationStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetViolationStatus(device, perfPolicyType, violTime);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetIrqNum(nvmlDevice_t device, unsigned int * irqNum) {
    if (!nvml_dispatch.nvmlDeviceGetIrqNum) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetIrqNum(device, irqNum);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNumGpuCores(nvmlDevice_t device, unsigned int * numCores) {
    if (!nvml_dispatch.nvmlDeviceGetNumGpuCores) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNumGpuCores(device, numCores);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPowerSource(nvmlDevice_t device, nvmlPowerSource_t * powerSource) {
    if (!nvml_dispatch.nvmlDeviceGetPowerSource) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPowerSource(device, powerSource);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMemoryBusWidth(nvmlDevice_t device, unsigned int * busWidth) {
    if (!nvml_dispatch.nvmlDeviceGetMemoryBusWidth) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMemoryBusWidth(device, busWidth);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPcieLinkMaxSpeed(nvmlDevice_t device, unsigned int * maxSpeed) {
    if (!nvml_dispatch.nvmlDeviceGetPcieLinkMaxSpeed) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPcieLinkMaxSpeed(device, maxSpeed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPcieSpeed(nvmlDevice_t device, unsigned int * pcieSpeed) {
    if (!nvml_dispatch.nvmlDeviceGetPcieSpeed) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPcieSpeed(device, pcieSpeed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAdaptiveClockInfoStatus(nvmlDevice_t device, unsigned int * adaptiveClockStatus) {
    if (!nvml_dispatch.nvmlDeviceGetAdaptiveClockInfoStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAdaptiveClockInfoStatus(device, adaptiveClockStatus);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetBusType(nvmlDevice_t device, nvmlBusType_t * type) {
    if (!nvml_dispatch.nvmlDeviceGetBusType) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetBusType(device, type);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuFabricInfo(nvmlDevice_t device, nvmlGpuFabricInfo_t * gpuFabricInfo) {
    if (!nvml_dispatch.nvmlDeviceGetGpuFabricInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuFabricInfo(device, gpuFabricInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuFabricInfoV(nvmlDevice_t device, nvmlGpuFabricInfoV_t * gpuFabricInfo) {
    if (!nvml_dispatch.nvmlDeviceGetGpuFabricInfoV) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuFabricInfoV(device, gpuFabricInfo);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetConfComputeCapabilities(nvmlConfComputeSystemCaps_t * capabilities) {
    if (!nvml_dispatch.nvmlSystemGetConfComputeCapabilities) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetConfComputeCapabilities(capabilities);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetConfComputeState(nvmlConfComputeSystemState_t * state) {
    if (!nvml_dispatch.nvmlSystemGetConfComputeState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetConfComputeState(state);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetConfComputeMemSizeInfo(nvmlDevice_t device, nvmlConfComputeMemSizeInfo_t * memInfo) {
    if (!nvml_dispatch.nvmlDeviceGetConfComputeMemSizeInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetConfComputeMemSizeInfo(device, memInfo);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetConfComputeGpusReadyState(unsigned int * isAcceptingWork) {
    if (!nvml_dispatch.nvmlSystemGetConfComputeGpusReadyState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetConfComputeGpusReadyState(isAcceptingWork);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetConfComputeProtectedMemoryUsage(nvmlDevice_t device, nvmlMemory_t * memory) {
    if (!nvml_dispatch.nvmlDeviceGetConfComputeProtectedMemoryUsage) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetConfComputeProtectedMemoryUsage(device, memory);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetConfComputeGpuCertificate(nvmlDevice_t device, nvmlConfComputeGpuCertificate_t * gpuCert) {
    if (!nvml_dispatch.nvmlDeviceGetConfComputeGpuCertificate) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetConfComputeGpuCertificate(device, gpuCert);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetConfComputeGpuAttestationReport(nvmlDevice_t device, nvmlConfComputeGpuAttestationReport_t * gpuAtstReport) {
    if (!nvml_dispatch.nvmlDeviceGetConfComputeGpuAttestationReport) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetConfComputeGpuAttestationReport(device, gpuAtstReport);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetConfComputeKeyRotationThresholdInfo(nvmlConfComputeGetKeyRotationThresholdInfo_t * pKeyRotationThrInfo) {
    if (!nvml_dispatch.nvmlSystemGetConfComputeKeyRotationThresholdInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetConfComputeKeyRotationThresholdInfo(pKeyRotationThrInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetConfComputeUnprotectedMemSize(nvmlDevice_t device, unsigned long long sizeKiB) {
    if (!nvml_dispatch.nvmlDeviceSetConfComputeUnprotectedMemSize) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetConfComputeUnprotectedMemSize(device, sizeKiB);
}

static inline nvmlReturn_t dispatch_nvmlSystemSetConfComputeGpusReadyState(unsigned int isAcceptingWork) {
    if (!nvml_dispatch.nvmlSystemSetConfComputeGpusReadyState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemSetConfComputeGpusReadyState(isAcceptingWork);
}

static inline nvmlReturn_t dispatch_nvmlSystemSetConfComputeKeyRotationThresholdInfo(nvmlConfComputeSetKeyRotationThresholdInfo_t * pKeyRotationThrInfo) {
    if (!nvml_dispatch.nvmlSystemSetConfComputeKeyRotationThresholdInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemSetConfComputeKeyRotationThresholdInfo(pKeyRotationThrInfo);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetConfComputeSettings(nvmlSystemConfComputeSettings_t * settings) {
    if (!nvml_dispatch.nvmlSystemGetConfComputeSettings) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetConfComputeSettings(settings);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGspFirmwareVersion(nvmlDevice_t device, char * version) {
    if (!nvml_dispatch.nvmlDeviceGetGspFirmwareVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGspFirmwareVersion(device, version);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGspFirmwareMode(nvmlDevice_t device, unsigned int * isEnabled, unsigned int * defaultMode) {
    if (!nvml_dispatch.nvmlDeviceGetGspFirmwareMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGspFirmwareMode(device, isEnabled, defaultMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSramEccErrorStatus(nvmlDevice_t device, nvmlEccSramErrorStatus_t * status) {
    if (!nvml_dispatch.nvmlDeviceGetSramEccErrorStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSramEccErrorStatus(device, status);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetPowerManagementLimit_v2(nvmlDevice_t device, nvmlPowerValue_v2_t * powerValue) {
    if (!nvml_dispatch.nvmlDeviceSetPowerManagementLimit_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetPowerManagementLimit_v2(device, powerValue);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAccountingMode(nvmlDevice_t device, nvmlEnableState_t * mode) {
    if (!nvml_dispatch.nvmlDeviceGetAccountingMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAccountingMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAccountingStats(nvmlDevice_t device, unsigned int pid, nvmlAccountingStats_t * stats) {
    if (!nvml_dispatch.nvmlDeviceGetAccountingStats) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAccountingStats(device, pid, stats);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAccountingPids(nvmlDevice_t device, unsigned int * count, unsigned int * pids) {
    if (!nvml_dispatch.nvmlDeviceGetAccountingPids) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAccountingPids(device, count, pids);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAccountingBufferSize(nvmlDevice_t device, unsigned int * bufferSize) {
    if (!nvml_dispatch.nvmlDeviceGetAccountingBufferSize) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAccountingBufferSize(device, bufferSize);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetRetiredPages(nvmlDevice_t device, nvmlPageRetirementCause_t cause, unsigned int * pageCount, unsigned long long * addresses) {
    if (!nvml_dispatch.nvmlDeviceGetRetiredPages) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetRetiredPages(device, cause, pageCount, addresses);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetRetiredPages_v2(nvmlDevice_t device, nvmlPageRetirementCause_t cause, unsigned int * pageCount, unsigned long long * addresses, unsigned long long * timestamps) {
    if (!nvml_dispatch.nvmlDeviceGetRetiredPages_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetRetiredPages_v2(device, cause, pageCount, addresses, timestamps);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetRetiredPagesPendingStatus(nvmlDevice_t device, nvmlEnableState_t * isPending) {
    if (!nvml_dispatch.nvmlDeviceGetRetiredPagesPendingStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetRetiredPagesPendingStatus(device, isPending);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetRemappedRows(nvmlDevice_t device, unsigned int * corrRows, unsigned int * uncRows, unsigned int * isPending, unsigned int * failureOccurred) {
    if (!nvml_dispatch.nvmlDeviceGetRemappedRows) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetRemappedRows(device, corrRows, uncRows, isPending, failureOccurred);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetRowRemapperHistogram(nvmlDevice_t device, nvmlRowRemapperHistogramValues_t * values) {
    if (!nvml_dispatch.nvmlDeviceGetRowRemapperHistogram) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetRowRemapperHistogram(device, values);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetArchitecture(nvmlDevice_t device, nvmlDeviceArchitecture_t * arch) {
    if (!nvml_dispatch.nvmlDeviceGetArchitecture) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetArchitecture(device, arch);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetClkMonStatus(nvmlDevice_t device, nvmlClkMonStatus_t * status) {
    if (!nvml_dispatch.nvmlDeviceGetClkMonStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetClkMonStatus(device, status);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetProcessUtilization(nvmlDevice_t device, nvmlProcessUtilizationSample_t * utilization, unsigned int * processSamplesCount, unsigned long long lastSeenTimeStamp) {
    if (!nvml_dispatch.nvmlDeviceGetProcessUtilization) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetProcessUtilization(device, utilization, processSamplesCount, lastSeenTimeStamp);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetProcessesUtilizationInfo(nvmlDevice_t device, nvmlProcessesUtilizationInfo_t * procesesUtilInfo) {
    if (!nvml_dispatch.nvmlDeviceGetProcessesUtilizationInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetProcessesUtilizationInfo(device, procesesUtilInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPlatformInfo(nvmlDevice_t device, nvmlPlatformInfo_t * platformInfo) {
    if (!nvml_dispatch.nvmlDeviceGetPlatformInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPlatformInfo(device, platformInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPdi(nvmlDevice_t device, nvmlPdi_t * pdi) {
    if (!nvml_dispatch.nvmlDeviceGetPdi) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPdi(device, pdi);
}

static inline nvmlReturn_t dispatch_nvmlUnitSetLedState(nvmlUnit_t unit, nvmlLedColor_t color) {
    if (!nvml_dispatch.nvmlUnitSetLedState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlUnitSetLedState(unit, color);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetPersistenceMode(nvmlDevice_t device, nvmlEnableState_t mode) {
    if (!nvml_dispatch.nvmlDeviceSetPersistenceMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetPersistenceMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetComputeMode(nvmlDevice_t device, nvmlComputeMode_t mode) {
    if (!nvml_dispatch.nvmlDeviceSetComputeMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetComputeMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetEccMode(nvmlDevice_t device, nvmlEnableState_t ecc) {
    if (!nvml_dispatch.nvmlDeviceSetEccMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetEccMode(device, ecc);
}

static inline nvmlReturn_t dispatch_nvmlDeviceClearEccErrorCounts(nvmlDevice_t device, nvmlEccCounterType_t counterType) {
    if (!nvml_dispatch.nvmlDeviceClearEccErrorCounts) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceClearEccErrorCounts(device, counterType);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetDriverModel(nvmlDevice_t device, nvmlDriverModel_t driverModel, unsigned int flags) {
    if (!nvml_dispatch.nvmlDeviceSetDriverModel) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetDriverModel(device, driverModel, flags);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetGpuLockedClocks(nvmlDevice_t device, unsigned int minGpuClockMHz, unsigned int maxGpuClockMHz) {
    if (!nvml_dispatch.nvmlDeviceSetGpuLockedClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetGpuLockedClocks(device, minGpuClockMHz, maxGpuClockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceResetGpuLockedClocks(nvmlDevice_t device) {
    if (!nvml_dispatch.nvmlDeviceResetGpuLockedClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceResetGpuLockedClocks(device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetMemoryLockedClocks(nvmlDevice_t device, unsigned int minMemClockMHz, unsigned int maxMemClockMHz) {
    if (!nvml_dispatch.nvmlDeviceSetMemoryLockedClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetMemoryLockedClocks(device, minMemClockMHz, maxMemClockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceResetMemoryLockedClocks(nvmlDevice_t device) {
    if (!nvml_dispatch.nvmlDeviceResetMemoryLockedClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceResetMemoryLockedClocks(device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetApplicationsClocks(nvmlDevice_t device, unsigned int memClockMHz, unsigned int graphicsClockMHz) {
    if (!nvml_dispatch.nvmlDeviceSetApplicationsClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetApplicationsClocks(device, memClockMHz, graphicsClockMHz);
}

static inline nvmlReturn_t dispatch_nvmlDeviceResetApplicationsClocks(nvmlDevice_t device) {
    if (!nvml_dispatch.nvmlDeviceResetApplicationsClocks) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceResetApplicationsClocks(device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetAutoBoostedClocksEnabled(nvmlDevice_t device, nvmlEnableState_t enabled) {
    if (!nvml_dispatch.nvmlDeviceSetAutoBoostedClocksEnabled) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetAutoBoostedClocksEnabled(device, enabled);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetDefaultAutoBoostedClocksEnabled(nvmlDevice_t device, nvmlEnableState_t enabled, unsigned int flags) {
    if (!nvml_dispatch.nvmlDeviceSetDefaultAutoBoostedClocksEnabled) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetDefaultAutoBoostedClocksEnabled(device, enabled, flags);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetDefaultFanSpeed_v2(nvmlDevice_t device, unsigned int fan) {
    if (!nvml_dispatch.nvmlDeviceSetDefaultFanSpeed_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetDefaultFanSpeed_v2(device, fan);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetFanControlPolicy(nvmlDevice_t device, unsigned int fan, nvmlFanControlPolicy_t policy) {
    if (!nvml_dispatch.nvmlDeviceSetFanControlPolicy) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetFanControlPolicy(device, fan, policy);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetTemperatureThreshold(nvmlDevice_t device, nvmlTemperatureThresholds_t thresholdType, int * temp) {
    if (!nvml_dispatch.nvmlDeviceSetTemperatureThreshold) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetTemperatureThreshold(device, thresholdType, temp);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetPowerManagementLimit(nvmlDevice_t device, unsigned int limit) {
    if (!nvml_dispatch.nvmlDeviceSetPowerManagementLimit) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetPowerManagementLimit(device, limit);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetGpuOperationMode(nvmlDevice_t device, nvmlGpuOperationMode_t mode) {
    if (!nvml_dispatch.nvmlDeviceSetGpuOperationMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetGpuOperationMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetAPIRestriction(nvmlDevice_t device, nvmlRestrictedAPI_t apiType, nvmlEnableState_t isRestricted) {
    if (!nvml_dispatch.nvmlDeviceSetAPIRestriction) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetAPIRestriction(device, apiType, isRestricted);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetFanSpeed_v2(nvmlDevice_t device, unsigned int fan, unsigned int speed) {
    if (!nvml_dispatch.nvmlDeviceSetFanSpeed_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetFanSpeed_v2(device, fan, speed);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetGpcClkVfOffset(nvmlDevice_t device, int offset) {
    if (!nvml_dispatch.nvmlDeviceSetGpcClkVfOffset) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetGpcClkVfOffset(device, offset);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetMemClkVfOffset(nvmlDevice_t device, int offset) {
    if (!nvml_dispatch.nvmlDeviceSetMemClkVfOffset) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetMemClkVfOffset(device, offset);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetAccountingMode(nvmlDevice_t device, nvmlEnableState_t mode) {
    if (!nvml_dispatch.nvmlDeviceSetAccountingMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetAccountingMode(device, mode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceClearAccountingPids(nvmlDevice_t device) {
    if (!nvml_dispatch.nvmlDeviceClearAccountingPids) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceClearAccountingPids(device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkState(nvmlDevice_t device, unsigned int link, nvmlEnableState_t * isActive) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkState(device, link, isActive);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkVersion(nvmlDevice_t device, unsigned int link, unsigned int * version) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkVersion(device, link, version);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkCapability(nvmlDevice_t device, unsigned int link, nvmlNvLinkCapability_t capability, unsigned int * capResult) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkCapability) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkCapability(device, link, capability, capResult);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkRemotePciInfo_v2(nvmlDevice_t device, unsigned int link, nvmlPciInfo_t * pci) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkRemotePciInfo_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkRemotePciInfo_v2(device, link, pci);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkErrorCounter(nvmlDevice_t device, unsigned int link, nvmlNvLinkErrorCounter_t counter, unsigned long long * counterValue) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkErrorCounter) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkErrorCounter(device, link, counter, counterValue);
}

static inline nvmlReturn_t dispatch_nvmlDeviceResetNvLinkErrorCounters(nvmlDevice_t device, unsigned int link) {
    if (!nvml_dispatch.nvmlDeviceResetNvLinkErrorCounters) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceResetNvLinkErrorCounters(device, link);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetNvLinkUtilizationControl(nvmlDevice_t device, unsigned int link, unsigned int counter, nvmlNvLinkUtilizationControl_t * control, unsigned int reset) {
    if (!nvml_dispatch.nvmlDeviceSetNvLinkUtilizationControl) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetNvLinkUtilizationControl(device, link, counter, control, reset);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkUtilizationControl(nvmlDevice_t device, unsigned int link, unsigned int counter, nvmlNvLinkUtilizationControl_t * control) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkUtilizationControl) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkUtilizationControl(device, link, counter, control);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkUtilizationCounter(nvmlDevice_t device, unsigned int link, unsigned int counter, unsigned long long * rxcounter, unsigned long long * txcounter) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkUtilizationCounter) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkUtilizationCounter(device, link, counter, rxcounter, txcounter);
}

static inline nvmlReturn_t dispatch_nvmlDeviceFreezeNvLinkUtilizationCounter(nvmlDevice_t device, unsigned int link, unsigned int counter, nvmlEnableState_t freeze) {
    if (!nvml_dispatch.nvmlDeviceFreezeNvLinkUtilizationCounter) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceFreezeNvLinkUtilizationCounter(device, link, counter, freeze);
}

static inline nvmlReturn_t dispatch_nvmlDeviceResetNvLinkUtilizationCounter(nvmlDevice_t device, unsigned int link, unsigned int counter) {
    if (!nvml_dispatch.nvmlDeviceResetNvLinkUtilizationCounter) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceResetNvLinkUtilizationCounter(device, link, counter);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkRemoteDeviceType(nvmlDevice_t device, unsigned int link, nvmlIntNvLinkDeviceType_t * pNvLinkDeviceType) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkRemoteDeviceType) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkRemoteDeviceType(device, link, pNvLinkDeviceType);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetNvLinkDeviceLowPowerThreshold(nvmlDevice_t device, nvmlNvLinkPowerThres_t * info) {
    if (!nvml_dispatch.nvmlDeviceSetNvLinkDeviceLowPowerThreshold) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetNvLinkDeviceLowPowerThreshold(device, info);
}

static inline nvmlReturn_t dispatch_nvmlSystemSetNvlinkBwMode(unsigned int nvlinkBwMode) {
    if (!nvml_dispatch.nvmlSystemSetNvlinkBwMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemSetNvlinkBwMode(nvlinkBwMode);
}

static inline nvmlReturn_t dispatch_nvmlSystemGetNvlinkBwMode(unsigned int * nvlinkBwMode) {
    if (!nvml_dispatch.nvmlSystemGetNvlinkBwMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemGetNvlinkBwMode(nvlinkBwMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvlinkSupportedBwModes(nvmlDevice_t device, nvmlNvlinkSupportedBwModes_t * supportedBwMode) {
    if (!nvml_dispatch.nvmlDeviceGetNvlinkSupportedBwModes) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvlinkSupportedBwModes(device, supportedBwMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvlinkBwMode(nvmlDevice_t device, nvmlNvlinkGetBwMode_t * getBwMode) {
    if (!nvml_dispatch.nvmlDeviceGetNvlinkBwMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvlinkBwMode(device, getBwMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetNvlinkBwMode(nvmlDevice_t device, nvmlNvlinkSetBwMode_t * setBwMode) {
    if (!nvml_dispatch.nvmlDeviceSetNvlinkBwMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetNvlinkBwMode(device, setBwMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkInfo(nvmlDevice_t device, nvmlNvLinkInfo_t * info) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkInfo(device, info);
}

static inline nvmlReturn_t dispatch_nvmlEventSetCreate(nvmlEventSet_t * set) {
    if (!nvml_dispatch.nvmlEventSetCreate) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlEventSetCreate(set);
}

static inline nvmlReturn_t dispatch_nvmlDeviceRegisterEvents(nvmlDevice_t device, unsigned long long eventTypes, nvmlEventSet_t set) {
    if (!nvml_dispatch.nvmlDeviceRegisterEvents) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceRegisterEvents(device, eventTypes, set);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSupportedEventTypes(nvmlDevice_t device, unsigned long long * eventTypes) {
    if (!nvml_dispatch.nvmlDeviceGetSupportedEventTypes) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSupportedEventTypes(device, eventTypes);
}

static inline nvmlReturn_t dispatch_nvmlEventSetWait_v2(nvmlEventSet_t set, nvmlEventData_t * data, unsigned int timeoutms) {
    if (!nvml_dispatch.nvmlEventSetWait_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlEventSetWait_v2(set, data, timeoutms);
}

static inline nvmlReturn_t dispatch_nvmlEventSetFree(nvmlEventSet_t set) {
    if (!nvml_dispatch.nvmlEventSetFree) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlEventSetFree(set);
}

static inline nvmlReturn_t dispatch_nvmlSystemEventSetCreate(nvmlSystemEventSetCreateRequest_t * request) {
    if (!nvml_dispatch.nvmlSystemEventSetCreate) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemEventSetCreate(request);
}

static inline nvmlReturn_t dispatch_nvmlSystemEventSetFree(nvmlSystemEventSetFreeRequest_t * request) {
    if (!nvml_dispatch.nvmlSystemEventSetFree) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemEventSetFree(request);
}

static inline nvmlReturn_t dispatch_nvmlSystemRegisterEvents(nvmlSystemRegisterEventRequest_t * request) {
    if (!nvml_dispatch.nvmlSystemRegisterEvents) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemRegisterEvents(request);
}

static inline nvmlReturn_t dispatch_nvmlSystemEventSetWait(nvmlSystemEventSetWaitRequest_t * request) {
    if (!nvml_dispatch.nvmlSystemEventSetWait) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSystemEventSetWait(request);
}

static inline nvmlReturn_t dispatch_nvmlDeviceModifyDrainState(nvmlPciInfo_t * pciInfo, nvmlEnableState_t newState) {
    if (!nvml_dispatch.nvmlDeviceModifyDrainState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceModifyDrainState(pciInfo, newState);
}

static inline nvmlReturn_t dispatch_nvmlDeviceQueryDrainState(nvmlPciInfo_t * pciInfo, nvmlEnableState_t * currentState) {
    if (!nvml_dispatch.nvmlDeviceQueryDrainState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceQueryDrainState(pciInfo, currentState);
}

static inline nvmlReturn_t dispatch_nvmlDeviceRemoveGpu_v2(nvmlPciInfo_t * pciInfo, nvmlDetachGpuState_t gpuState, nvmlPcieLinkState_t linkState) {
    if (!nvml_dispatch.nvmlDeviceRemoveGpu_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceRemoveGpu_v2(pciInfo, gpuState, linkState);
}

static inline nvmlReturn_t dispatch_nvmlDeviceDiscoverGpus(nvmlPciInfo_t * pciInfo) {
    if (!nvml_dispatch.nvmlDeviceDiscoverGpus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceDiscoverGpus(pciInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetFieldValues(nvmlDevice_t device, int valuesCount, nvmlFieldValue_t * values) {
    if (!nvml_dispatch.nvmlDeviceGetFieldValues) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetFieldValues(device, valuesCount, values);
}

static inline nvmlReturn_t dispatch_nvmlDeviceClearFieldValues(nvmlDevice_t device, int valuesCount, nvmlFieldValue_t * values) {
    if (!nvml_dispatch.nvmlDeviceClearFieldValues) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceClearFieldValues(device, valuesCount, values);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVirtualizationMode(nvmlDevice_t device, nvmlGpuVirtualizationMode_t * pVirtualMode) {
    if (!nvml_dispatch.nvmlDeviceGetVirtualizationMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVirtualizationMode(device, pVirtualMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHostVgpuMode(nvmlDevice_t device, nvmlHostVgpuMode_t * pHostVgpuMode) {
    if (!nvml_dispatch.nvmlDeviceGetHostVgpuMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHostVgpuMode(device, pHostVgpuMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetVirtualizationMode(nvmlDevice_t device, nvmlGpuVirtualizationMode_t virtualMode) {
    if (!nvml_dispatch.nvmlDeviceSetVirtualizationMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetVirtualizationMode(device, virtualMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuHeterogeneousMode(nvmlDevice_t device, nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuHeterogeneousMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuHeterogeneousMode(device, pHeterogeneousMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetVgpuHeterogeneousMode(nvmlDevice_t device, const nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode) {
    if (!nvml_dispatch.nvmlDeviceSetVgpuHeterogeneousMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetVgpuHeterogeneousMode(device, pHeterogeneousMode);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetPlacementId(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuPlacementId_t * pPlacement) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetPlacementId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetPlacementId(vgpuInstance, pPlacement);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuTypeSupportedPlacements(nvmlDevice_t device, nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuPlacementList_t * pPlacementList) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuTypeSupportedPlacements) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuTypeSupportedPlacements(device, vgpuTypeId, pPlacementList);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuTypeCreatablePlacements(nvmlDevice_t device, nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuPlacementList_t * pPlacementList) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuTypeCreatablePlacements) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuTypeCreatablePlacements(device, vgpuTypeId, pPlacementList);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetGspHeapSize(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * gspHeapSize) {
    if (!nvml_dispatch.nvmlVgpuTypeGetGspHeapSize) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetGspHeapSize(vgpuTypeId, gspHeapSize);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetFbReservation(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * fbReservation) {
    if (!nvml_dispatch.nvmlVgpuTypeGetFbReservation) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetFbReservation(vgpuTypeId, fbReservation);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetRuntimeStateSize(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuRuntimeState_t * pState) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetRuntimeStateSize) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetRuntimeStateSize(vgpuInstance, pState);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetVgpuCapabilities(nvmlDevice_t device, nvmlDeviceVgpuCapability_t capability, nvmlEnableState_t state) {
    if (!nvml_dispatch.nvmlDeviceSetVgpuCapabilities) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetVgpuCapabilities(device, capability, state);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGridLicensableFeatures_v4(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures) {
    if (!nvml_dispatch.nvmlDeviceGetGridLicensableFeatures_v4) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGridLicensableFeatures_v4(device, pGridLicensableFeatures);
}

static inline nvmlReturn_t dispatch_nvmlGetVgpuDriverCapabilities(nvmlVgpuDriverCapability_t capability, unsigned int * capResult) {
    if (!nvml_dispatch.nvmlGetVgpuDriverCapabilities) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGetVgpuDriverCapabilities(capability, capResult);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuCapabilities(nvmlDevice_t device, nvmlDeviceVgpuCapability_t capability, unsigned int * capResult) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuCapabilities) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuCapabilities(device, capability, capResult);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSupportedVgpus(nvmlDevice_t device, unsigned int * vgpuCount, nvmlVgpuTypeId_t * vgpuTypeIds) {
    if (!nvml_dispatch.nvmlDeviceGetSupportedVgpus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSupportedVgpus(device, vgpuCount, vgpuTypeIds);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCreatableVgpus(nvmlDevice_t device, unsigned int * vgpuCount, nvmlVgpuTypeId_t * vgpuTypeIds) {
    if (!nvml_dispatch.nvmlDeviceGetCreatableVgpus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCreatableVgpus(device, vgpuCount, vgpuTypeIds);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetClass(nvmlVgpuTypeId_t vgpuTypeId, char * vgpuTypeClass, unsigned int * size) {
    if (!nvml_dispatch.nvmlVgpuTypeGetClass) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetClass(vgpuTypeId, vgpuTypeClass, size);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetName(nvmlVgpuTypeId_t vgpuTypeId, char * vgpuTypeName, unsigned int * size) {
    if (!nvml_dispatch.nvmlVgpuTypeGetName) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetName(vgpuTypeId, vgpuTypeName, size);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetGpuInstanceProfileId(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * gpuInstanceProfileId) {
    if (!nvml_dispatch.nvmlVgpuTypeGetGpuInstanceProfileId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetGpuInstanceProfileId(vgpuTypeId, gpuInstanceProfileId);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetDeviceID(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * deviceID, unsigned long long * subsystemID) {
    if (!nvml_dispatch.nvmlVgpuTypeGetDeviceID) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetDeviceID(vgpuTypeId, deviceID, subsystemID);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetFramebufferSize(nvmlVgpuTypeId_t vgpuTypeId, unsigned long long * fbSize) {
    if (!nvml_dispatch.nvmlVgpuTypeGetFramebufferSize) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetFramebufferSize(vgpuTypeId, fbSize);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetNumDisplayHeads(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * numDisplayHeads) {
    if (!nvml_dispatch.nvmlVgpuTypeGetNumDisplayHeads) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetNumDisplayHeads(vgpuTypeId, numDisplayHeads);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetResolution(nvmlVgpuTypeId_t vgpuTypeId, unsigned int displayIndex, unsigned int * xdim, unsigned int * ydim) {
    if (!nvml_dispatch.nvmlVgpuTypeGetResolution) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetResolution(vgpuTypeId, displayIndex, xdim, ydim);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetLicense(nvmlVgpuTypeId_t vgpuTypeId, char * vgpuTypeLicenseString, unsigned int size) {
    if (!nvml_dispatch.nvmlVgpuTypeGetLicense) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetLicense(vgpuTypeId, vgpuTypeLicenseString, size);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetFrameRateLimit(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * frameRateLimit) {
    if (!nvml_dispatch.nvmlVgpuTypeGetFrameRateLimit) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetFrameRateLimit(vgpuTypeId, frameRateLimit);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetMaxInstances(nvmlDevice_t device, nvmlVgpuTypeId_t vgpuTypeId, unsigned int * vgpuInstanceCount) {
    if (!nvml_dispatch.nvmlVgpuTypeGetMaxInstances) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetMaxInstances(device, vgpuTypeId, vgpuInstanceCount);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetMaxInstancesPerVm(nvmlVgpuTypeId_t vgpuTypeId, unsigned int * vgpuInstanceCountPerVm) {
    if (!nvml_dispatch.nvmlVgpuTypeGetMaxInstancesPerVm) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetMaxInstancesPerVm(vgpuTypeId, vgpuInstanceCountPerVm);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetBAR1Info(nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuTypeBar1Info_t * bar1Info) {
    if (!nvml_dispatch.nvmlVgpuTypeGetBAR1Info) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetBAR1Info(vgpuTypeId, bar1Info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetActiveVgpus(nvmlDevice_t device, unsigned int * vgpuCount, nvmlVgpuInstance_t * vgpuInstances) {
    if (!nvml_dispatch.nvmlDeviceGetActiveVgpus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetActiveVgpus(device, vgpuCount, vgpuInstances);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetVmID(nvmlVgpuInstance_t vgpuInstance, char * vmId, unsigned int size, nvmlVgpuVmIdType_t * vmIdType) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetVmID) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetVmID(vgpuInstance, vmId, size, vmIdType);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetUUID(nvmlVgpuInstance_t vgpuInstance, char * uuid, unsigned int size) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetUUID) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetUUID(vgpuInstance, uuid, size);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetVmDriverVersion(nvmlVgpuInstance_t vgpuInstance, char* version, unsigned int length) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetVmDriverVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetVmDriverVersion(vgpuInstance, version, length);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetFbUsage(nvmlVgpuInstance_t vgpuInstance, unsigned long long * fbUsage) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetFbUsage) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetFbUsage(vgpuInstance, fbUsage);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetLicenseStatus(nvmlVgpuInstance_t vgpuInstance, unsigned int * licensed) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetLicenseStatus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetLicenseStatus(vgpuInstance, licensed);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetType(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuTypeId_t * vgpuTypeId) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetType) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetType(vgpuInstance, vgpuTypeId);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetFrameRateLimit(nvmlVgpuInstance_t vgpuInstance, unsigned int * frameRateLimit) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetFrameRateLimit) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetFrameRateLimit(vgpuInstance, frameRateLimit);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetEccMode(nvmlVgpuInstance_t vgpuInstance, nvmlEnableState_t * eccMode) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetEccMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetEccMode(vgpuInstance, eccMode);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetEncoderCapacity(nvmlVgpuInstance_t vgpuInstance, unsigned int * encoderCapacity) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetEncoderCapacity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetEncoderCapacity(vgpuInstance, encoderCapacity);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceSetEncoderCapacity(nvmlVgpuInstance_t vgpuInstance, unsigned int encoderCapacity) {
    if (!nvml_dispatch.nvmlVgpuInstanceSetEncoderCapacity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceSetEncoderCapacity(vgpuInstance, encoderCapacity);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetEncoderStats(nvmlVgpuInstance_t vgpuInstance, unsigned int * sessionCount, unsigned int * averageFps, unsigned int * averageLatency) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetEncoderStats) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetEncoderStats(vgpuInstance, sessionCount, averageFps, averageLatency);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetEncoderSessions(nvmlVgpuInstance_t vgpuInstance, unsigned int * sessionCount, nvmlEncoderSessionInfo_t * sessionInfo) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetEncoderSessions) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetEncoderSessions(vgpuInstance, sessionCount, sessionInfo);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetFBCStats(nvmlVgpuInstance_t vgpuInstance, nvmlFBCStats_t * fbcStats) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetFBCStats) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetFBCStats(vgpuInstance, fbcStats);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetFBCSessions(nvmlVgpuInstance_t vgpuInstance, unsigned int * sessionCount, nvmlFBCSessionInfo_t * sessionInfo) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetFBCSessions) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetFBCSessions(vgpuInstance, sessionCount, sessionInfo);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetGpuInstanceId(nvmlVgpuInstance_t vgpuInstance, unsigned int * gpuInstanceId) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetGpuInstanceId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetGpuInstanceId(vgpuInstance, gpuInstanceId);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetGpuPciId(nvmlVgpuInstance_t vgpuInstance, char * vgpuPciId, unsigned int * length) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetGpuPciId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetGpuPciId(vgpuInstance, vgpuPciId, length);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetCapabilities(nvmlVgpuTypeId_t vgpuTypeId, nvmlVgpuCapability_t capability, unsigned int * capResult) {
    if (!nvml_dispatch.nvmlVgpuTypeGetCapabilities) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetCapabilities(vgpuTypeId, capability, capResult);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetMdevUUID(nvmlVgpuInstance_t vgpuInstance, char * mdevUuid, unsigned int size) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetMdevUUID) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetMdevUUID(vgpuInstance, mdevUuid, size);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetCreatableVgpus(nvmlGpuInstance_t gpuInstance, nvmlVgpuTypeIdInfo_t * pVgpus) {
    if (!nvml_dispatch.nvmlGpuInstanceGetCreatableVgpus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetCreatableVgpus(gpuInstance, pVgpus);
}

static inline nvmlReturn_t dispatch_nvmlVgpuTypeGetMaxInstancesPerGpuInstance(nvmlVgpuTypeMaxInstance_t * pMaxInstance) {
    if (!nvml_dispatch.nvmlVgpuTypeGetMaxInstancesPerGpuInstance) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuTypeGetMaxInstancesPerGpuInstance(pMaxInstance);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetActiveVgpus(nvmlGpuInstance_t gpuInstance, nvmlActiveVgpuInstanceInfo_t * pVgpuInstanceInfo) {
    if (!nvml_dispatch.nvmlGpuInstanceGetActiveVgpus) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetActiveVgpus(gpuInstance, pVgpuInstanceInfo);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceSetVgpuSchedulerState(nvmlGpuInstance_t gpuInstance, nvmlVgpuSchedulerState_t * pScheduler) {
    if (!nvml_dispatch.nvmlGpuInstanceSetVgpuSchedulerState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceSetVgpuSchedulerState(gpuInstance, pScheduler);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetVgpuSchedulerState(nvmlGpuInstance_t gpuInstance, nvmlVgpuSchedulerStateInfo_t * pSchedulerStateInfo) {
    if (!nvml_dispatch.nvmlGpuInstanceGetVgpuSchedulerState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetVgpuSchedulerState(gpuInstance, pSchedulerStateInfo);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetVgpuSchedulerLog(nvmlGpuInstance_t gpuInstance, nvmlVgpuSchedulerLogInfo_t * pSchedulerLogInfo) {
    if (!nvml_dispatch.nvmlGpuInstanceGetVgpuSchedulerLog) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetVgpuSchedulerLog(gpuInstance, pSchedulerLogInfo);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetVgpuTypeCreatablePlacements(nvmlGpuInstance_t gpuInstance, nvmlVgpuCreatablePlacementInfo_t * pCreatablePlacementInfo) {
    if (!nvml_dispatch.nvmlGpuInstanceGetVgpuTypeCreatablePlacements) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetVgpuTypeCreatablePlacements(gpuInstance, pCreatablePlacementInfo);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetVgpuHeterogeneousMode(nvmlGpuInstance_t gpuInstance, nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode) {
    if (!nvml_dispatch.nvmlGpuInstanceGetVgpuHeterogeneousMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetVgpuHeterogeneousMode(gpuInstance, pHeterogeneousMode);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceSetVgpuHeterogeneousMode(nvmlGpuInstance_t gpuInstance, const nvmlVgpuHeterogeneousMode_t * pHeterogeneousMode) {
    if (!nvml_dispatch.nvmlGpuInstanceSetVgpuHeterogeneousMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceSetVgpuHeterogeneousMode(gpuInstance, pHeterogeneousMode);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetMetadata(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuMetadata_t * vgpuMetadata, unsigned int * bufferSize) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetMetadata) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetMetadata(vgpuInstance, vgpuMetadata, bufferSize);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuMetadata(nvmlDevice_t device, nvmlVgpuPgpuMetadata_t * pgpuMetadata, unsigned int * bufferSize) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuMetadata) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuMetadata(device, pgpuMetadata, bufferSize);
}

static inline nvmlReturn_t dispatch_nvmlGetVgpuCompatibility(nvmlVgpuMetadata_t * vgpuMetadata, nvmlVgpuPgpuMetadata_t * pgpuMetadata, nvmlVgpuPgpuCompatibility_t * compatibilityInfo) {
    if (!nvml_dispatch.nvmlGetVgpuCompatibility) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGetVgpuCompatibility(vgpuMetadata, pgpuMetadata, compatibilityInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPgpuMetadataString(nvmlDevice_t device, char * pgpuMetadata, unsigned int * bufferSize) {
    if (!nvml_dispatch.nvmlDeviceGetPgpuMetadataString) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPgpuMetadataString(device, pgpuMetadata, bufferSize);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuSchedulerLog(nvmlDevice_t device, nvmlVgpuSchedulerLog_t * pSchedulerLog) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuSchedulerLog) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuSchedulerLog(device, pSchedulerLog);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuSchedulerState(nvmlDevice_t device, nvmlVgpuSchedulerGetState_t * pSchedulerState) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuSchedulerState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuSchedulerState(device, pSchedulerState);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuSchedulerCapabilities(nvmlDevice_t device, nvmlVgpuSchedulerCapabilities_t * pCapabilities) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuSchedulerCapabilities) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuSchedulerCapabilities(device, pCapabilities);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetVgpuSchedulerState(nvmlDevice_t device, nvmlVgpuSchedulerSetState_t * pSchedulerState) {
    if (!nvml_dispatch.nvmlDeviceSetVgpuSchedulerState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetVgpuSchedulerState(device, pSchedulerState);
}

static inline nvmlReturn_t dispatch_nvmlGetVgpuVersion(nvmlVgpuVersion_t * supported, nvmlVgpuVersion_t * current) {
    if (!nvml_dispatch.nvmlGetVgpuVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGetVgpuVersion(supported, current);
}

static inline nvmlReturn_t dispatch_nvmlSetVgpuVersion(nvmlVgpuVersion_t * vgpuVersion) {
    if (!nvml_dispatch.nvmlSetVgpuVersion) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlSetVgpuVersion(vgpuVersion);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuUtilization(nvmlDevice_t device, unsigned long long lastSeenTimeStamp, nvmlValueType_t * sampleValType, unsigned int * vgpuInstanceSamplesCount, nvmlVgpuInstanceUtilizationSample_t * utilizationSamples) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuUtilization) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuUtilization(device, lastSeenTimeStamp, sampleValType, vgpuInstanceSamplesCount, utilizationSamples);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuInstancesUtilizationInfo(nvmlDevice_t device, nvmlVgpuInstancesUtilizationInfo_t * vgpuUtilInfo) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuInstancesUtilizationInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuInstancesUtilizationInfo(device, vgpuUtilInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuProcessUtilization(nvmlDevice_t device, unsigned long long lastSeenTimeStamp, unsigned int * vgpuProcessSamplesCount, nvmlVgpuProcessUtilizationSample_t * utilizationSamples) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuProcessUtilization) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuProcessUtilization(device, lastSeenTimeStamp, vgpuProcessSamplesCount, utilizationSamples);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetVgpuProcessesUtilizationInfo(nvmlDevice_t device, nvmlVgpuProcessesUtilizationInfo_t * vgpuProcUtilInfo) {
    if (!nvml_dispatch.nvmlDeviceGetVgpuProcessesUtilizationInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetVgpuProcessesUtilizationInfo(device, vgpuProcUtilInfo);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetAccountingMode(nvmlVgpuInstance_t vgpuInstance, nvmlEnableState_t * mode) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetAccountingMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetAccountingMode(vgpuInstance, mode);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetAccountingPids(nvmlVgpuInstance_t vgpuInstance, unsigned int * count, unsigned int * pids) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetAccountingPids) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetAccountingPids(vgpuInstance, count, pids);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetAccountingStats(nvmlVgpuInstance_t vgpuInstance, unsigned int pid, nvmlAccountingStats_t * stats) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetAccountingStats) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetAccountingStats(vgpuInstance, pid, stats);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceClearAccountingPids(nvmlVgpuInstance_t vgpuInstance) {
    if (!nvml_dispatch.nvmlVgpuInstanceClearAccountingPids) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceClearAccountingPids(vgpuInstance);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetLicenseInfo_v2(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuLicenseInfo_t * licenseInfo) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetLicenseInfo_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetLicenseInfo_v2(vgpuInstance, licenseInfo);
}

static inline nvmlReturn_t dispatch_nvmlGetExcludedDeviceCount(unsigned int * deviceCount) {
    if (!nvml_dispatch.nvmlGetExcludedDeviceCount) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGetExcludedDeviceCount(deviceCount);
}

static inline nvmlReturn_t dispatch_nvmlGetExcludedDeviceInfoByIndex(unsigned int index, nvmlExcludedDeviceInfo_t * info) {
    if (!nvml_dispatch.nvmlGetExcludedDeviceInfoByIndex) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGetExcludedDeviceInfoByIndex(index, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceReadWritePRM_v1(nvmlDevice_t device, nvmlPRMTLV_v1_t * buffer) {
    if (!nvml_dispatch.nvmlDeviceReadWritePRM_v1) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceReadWritePRM_v1(device, buffer);
}

static inline nvmlReturn_t dispatch_nvmlDeviceSetMigMode(nvmlDevice_t device, unsigned int mode, nvmlReturn_t * activationStatus) {
    if (!nvml_dispatch.nvmlDeviceSetMigMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceSetMigMode(device, mode, activationStatus);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMigMode(nvmlDevice_t device, unsigned int * currentMode, unsigned int * pendingMode) {
    if (!nvml_dispatch.nvmlDeviceGetMigMode) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMigMode(device, currentMode, pendingMode);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstanceProfileInfo(nvmlDevice_t device, unsigned int profile, nvmlGpuInstanceProfileInfo_t * info) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstanceProfileInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstanceProfileInfo(device, profile, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstanceProfileInfoV(nvmlDevice_t device, unsigned int profile, nvmlGpuInstanceProfileInfo_v2_t * info) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstanceProfileInfoV) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstanceProfileInfoV(device, profile, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstanceProfileInfoByIdV(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstanceProfileInfo_v2_t * info) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstanceProfileInfoByIdV) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstanceProfileInfoByIdV(device, profileId, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstancePossiblePlacements_v2(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstancePlacement_t * placements, unsigned int * count) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstancePossiblePlacements_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstancePossiblePlacements_v2(device, profileId, placements, count);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstanceRemainingCapacity(nvmlDevice_t device, unsigned int profileId, unsigned int * count) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstanceRemainingCapacity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstanceRemainingCapacity(device, profileId, count);
}

static inline nvmlReturn_t dispatch_nvmlDeviceCreateGpuInstance(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstance_t * gpuInstance) {
    if (!nvml_dispatch.nvmlDeviceCreateGpuInstance) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceCreateGpuInstance(device, profileId, gpuInstance);
}

static inline nvmlReturn_t dispatch_nvmlDeviceCreateGpuInstanceWithPlacement(nvmlDevice_t device, unsigned int profileId, const nvmlGpuInstancePlacement_t * placement, nvmlGpuInstance_t * gpuInstance) {
    if (!nvml_dispatch.nvmlDeviceCreateGpuInstanceWithPlacement) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceCreateGpuInstanceWithPlacement(device, profileId, placement, gpuInstance);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceDestroy(nvmlGpuInstance_t gpuInstance) {
    if (!nvml_dispatch.nvmlGpuInstanceDestroy) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceDestroy(gpuInstance);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstances(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstance_t * gpuInstances, unsigned int * count) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstances) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstances(device, profileId, gpuInstances, count);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstanceById(nvmlDevice_t device, unsigned int id, nvmlGpuInstance_t * gpuInstance) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstanceById) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstanceById(device, id, gpuInstance);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetInfo(nvmlGpuInstance_t gpuInstance, nvmlGpuInstanceInfo_t * info) {
    if (!nvml_dispatch.nvmlGpuInstanceGetInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetInfo(gpuInstance, info);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetComputeInstanceProfileInfo(nvmlGpuInstance_t gpuInstance, unsigned int profile, unsigned int engProfile, nvmlComputeInstanceProfileInfo_t * info) {
    if (!nvml_dispatch.nvmlGpuInstanceGetComputeInstanceProfileInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetComputeInstanceProfileInfo(gpuInstance, profile, engProfile, info);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetComputeInstanceProfileInfoV(nvmlGpuInstance_t gpuInstance, unsigned int profile, unsigned int engProfile, nvmlComputeInstanceProfileInfo_v2_t * info) {
    if (!nvml_dispatch.nvmlGpuInstanceGetComputeInstanceProfileInfoV) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetComputeInstanceProfileInfoV(gpuInstance, profile, engProfile, info);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetComputeInstanceRemainingCapacity(nvmlGpuInstance_t gpuInstance, unsigned int profileId, unsigned int * count) {
    if (!nvml_dispatch.nvmlGpuInstanceGetComputeInstanceRemainingCapacity) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetComputeInstanceRemainingCapacity(gpuInstance, profileId, count);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetComputeInstancePossiblePlacements(nvmlGpuInstance_t gpuInstance, unsigned int profileId, nvmlComputeInstancePlacement_t * placements, unsigned int * count) {
    if (!nvml_dispatch.nvmlGpuInstanceGetComputeInstancePossiblePlacements) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetComputeInstancePossiblePlacements(gpuInstance, profileId, placements, count);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceCreateComputeInstance(nvmlGpuInstance_t gpuInstance, unsigned int profileId, nvmlComputeInstance_t * computeInstance) {
    if (!nvml_dispatch.nvmlGpuInstanceCreateComputeInstance) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceCreateComputeInstance(gpuInstance, profileId, computeInstance);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceCreateComputeInstanceWithPlacement(nvmlGpuInstance_t gpuInstance, unsigned int profileId, const nvmlComputeInstancePlacement_t * placement, nvmlComputeInstance_t * computeInstance) {
    if (!nvml_dispatch.nvmlGpuInstanceCreateComputeInstanceWithPlacement) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceCreateComputeInstanceWithPlacement(gpuInstance, profileId, placement, computeInstance);
}

static inline nvmlReturn_t dispatch_nvmlComputeInstanceDestroy(nvmlComputeInstance_t computeInstance) {
    if (!nvml_dispatch.nvmlComputeInstanceDestroy) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlComputeInstanceDestroy(computeInstance);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetComputeInstances(nvmlGpuInstance_t gpuInstance, unsigned int profileId, nvmlComputeInstance_t * computeInstances, unsigned int * count) {
    if (!nvml_dispatch.nvmlGpuInstanceGetComputeInstances) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetComputeInstances(gpuInstance, profileId, computeInstances, count);
}

static inline nvmlReturn_t dispatch_nvmlGpuInstanceGetComputeInstanceById(nvmlGpuInstance_t gpuInstance, unsigned int id, nvmlComputeInstance_t * computeInstance) {
    if (!nvml_dispatch.nvmlGpuInstanceGetComputeInstanceById) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpuInstanceGetComputeInstanceById(gpuInstance, id, computeInstance);
}

static inline nvmlReturn_t dispatch_nvmlComputeInstanceGetInfo_v2(nvmlComputeInstance_t computeInstance, nvmlComputeInstanceInfo_t * info) {
    if (!nvml_dispatch.nvmlComputeInstanceGetInfo_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlComputeInstanceGetInfo_v2(computeInstance, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceIsMigDeviceHandle(nvmlDevice_t device, unsigned int * isMigDevice) {
    if (!nvml_dispatch.nvmlDeviceIsMigDeviceHandle) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceIsMigDeviceHandle(device, isMigDevice);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstanceId(nvmlDevice_t device, unsigned int * id) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstanceId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstanceId(device, id);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetComputeInstanceId(nvmlDevice_t device, unsigned int * id) {
    if (!nvml_dispatch.nvmlDeviceGetComputeInstanceId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetComputeInstanceId(device, id);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMaxMigDeviceCount(nvmlDevice_t device, unsigned int * count) {
    if (!nvml_dispatch.nvmlDeviceGetMaxMigDeviceCount) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMaxMigDeviceCount(device, count);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMigDeviceHandleByIndex(nvmlDevice_t device, unsigned int index, nvmlDevice_t * migDevice) {
    if (!nvml_dispatch.nvmlDeviceGetMigDeviceHandleByIndex) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMigDeviceHandleByIndex(device, index, migDevice);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDeviceHandleFromMigDeviceHandle(nvmlDevice_t migDevice, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetDeviceHandleFromMigDeviceHandle) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDeviceHandleFromMigDeviceHandle(migDevice, device);
}

static inline nvmlReturn_t dispatch_nvmlGpmMetricsGet(nvmlGpmMetricsGet_t * metricsGet) {
    if (!nvml_dispatch.nvmlGpmMetricsGet) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmMetricsGet(metricsGet);
}

static inline nvmlReturn_t dispatch_nvmlGpmSampleFree(nvmlGpmSample_t gpmSample) {
    if (!nvml_dispatch.nvmlGpmSampleFree) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmSampleFree(gpmSample);
}

static inline nvmlReturn_t dispatch_nvmlGpmSampleAlloc(nvmlGpmSample_t * gpmSample) {
    if (!nvml_dispatch.nvmlGpmSampleAlloc) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmSampleAlloc(gpmSample);
}

static inline nvmlReturn_t dispatch_nvmlGpmSampleGet(nvmlDevice_t device, nvmlGpmSample_t gpmSample) {
    if (!nvml_dispatch.nvmlGpmSampleGet) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmSampleGet(device, gpmSample);
}

static inline nvmlReturn_t dispatch_nvmlGpmMigSampleGet(nvmlDevice_t device, unsigned int gpuInstanceId, nvmlGpmSample_t gpmSample) {
    if (!nvml_dispatch.nvmlGpmMigSampleGet) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmMigSampleGet(device, gpuInstanceId, gpmSample);
}

static inline nvmlReturn_t dispatch_nvmlGpmQueryDeviceSupport(nvmlDevice_t device, nvmlGpmSupport_t * gpmSupport) {
    if (!nvml_dispatch.nvmlGpmQueryDeviceSupport) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmQueryDeviceSupport(device, gpmSupport);
}

static inline nvmlReturn_t dispatch_nvmlGpmQueryIfStreamingEnabled(nvmlDevice_t device, unsigned int * state) {
    if (!nvml_dispatch.nvmlGpmQueryIfStreamingEnabled) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmQueryIfStreamingEnabled(device, state);
}

static inline nvmlReturn_t dispatch_nvmlGpmSetStreamingEnabled(nvmlDevice_t device, unsigned int state) {
    if (!nvml_dispatch.nvmlGpmSetStreamingEnabled) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlGpmSetStreamingEnabled(device, state);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCapabilities(nvmlDevice_t device, nvmlDeviceCapabilities_t * caps) {
    if (!nvml_dispatch.nvmlDeviceGetCapabilities) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCapabilities(device, caps);
}

static inline nvmlReturn_t dispatch_nvmlDeviceWorkloadPowerProfileGetProfilesInfo(nvmlDevice_t device, nvmlWorkloadPowerProfileProfilesInfo_t * profilesInfo) {
    if (!nvml_dispatch.nvmlDeviceWorkloadPowerProfileGetProfilesInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceWorkloadPowerProfileGetProfilesInfo(device, profilesInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceWorkloadPowerProfileGetCurrentProfiles(nvmlDevice_t device, nvmlWorkloadPowerProfileCurrentProfiles_t * currentProfiles) {
    if (!nvml_dispatch.nvmlDeviceWorkloadPowerProfileGetCurrentProfiles) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceWorkloadPowerProfileGetCurrentProfiles(device, currentProfiles);
}

static inline nvmlReturn_t dispatch_nvmlDeviceWorkloadPowerProfileSetRequestedProfiles(nvmlDevice_t device, nvmlWorkloadPowerProfileRequestedProfiles_t * requestedProfiles) {
    if (!nvml_dispatch.nvmlDeviceWorkloadPowerProfileSetRequestedProfiles) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceWorkloadPowerProfileSetRequestedProfiles(device, requestedProfiles);
}

static inline nvmlReturn_t dispatch_nvmlDeviceWorkloadPowerProfileClearRequestedProfiles(nvmlDevice_t device, nvmlWorkloadPowerProfileRequestedProfiles_t * requestedProfiles) {
    if (!nvml_dispatch.nvmlDeviceWorkloadPowerProfileClearRequestedProfiles) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceWorkloadPowerProfileClearRequestedProfiles(device, requestedProfiles);
}

static inline nvmlReturn_t dispatch_nvmlDevicePowerSmoothingActivatePresetProfile(nvmlDevice_t device, nvmlPowerSmoothingProfile_t * profile) {
    if (!nvml_dispatch.nvmlDevicePowerSmoothingActivatePresetProfile) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDevicePowerSmoothingActivatePresetProfile(device, profile);
}

static inline nvmlReturn_t dispatch_nvmlDevicePowerSmoothingUpdatePresetProfileParam(nvmlDevice_t device, nvmlPowerSmoothingProfile_t * profile) {
    if (!nvml_dispatch.nvmlDevicePowerSmoothingUpdatePresetProfileParam) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDevicePowerSmoothingUpdatePresetProfileParam(device, profile);
}

static inline nvmlReturn_t dispatch_nvmlDevicePowerSmoothingSetState(nvmlDevice_t device, nvmlPowerSmoothingState_t * state) {
    if (!nvml_dispatch.nvmlDevicePowerSmoothingSetState) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDevicePowerSmoothingSetState(device, state);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetSramUniqueUncorrectedEccErrorCounts(nvmlDevice_t device, nvmlEccSramUniqueUncorrectedErrorCounts_t * errorCounts) {
    if (!nvml_dispatch.nvmlDeviceGetSramUniqueUncorrectedEccErrorCounts) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetSramUniqueUncorrectedEccErrorCounts(device, errorCounts);
}

static inline nvmlReturn_t dispatch_nvmlInit(void) {
    if (!nvml_dispatch.nvmlInit) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlInit();
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetCount(unsigned int * deviceCount) {
    if (!nvml_dispatch.nvmlDeviceGetCount) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetCount(deviceCount);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHandleByIndex(unsigned int index, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetHandleByIndex) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHandleByIndex(index, device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetHandleByPciBusId(const char * pciBusId, nvmlDevice_t * device) {
    if (!nvml_dispatch.nvmlDeviceGetHandleByPciBusId) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetHandleByPciBusId(pciBusId, device);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPciInfo(nvmlDevice_t device, nvmlPciInfo_t * pci) {
    if (!nvml_dispatch.nvmlDeviceGetPciInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPciInfo(device, pci);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetPciInfo_v2(nvmlDevice_t device, nvmlPciInfo_t * pci) {
    if (!nvml_dispatch.nvmlDeviceGetPciInfo_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetPciInfo_v2(device, pci);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetNvLinkRemotePciInfo(nvmlDevice_t device, unsigned int link, nvmlPciInfo_t * pci) {
    if (!nvml_dispatch.nvmlDeviceGetNvLinkRemotePciInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetNvLinkRemotePciInfo(device, link, pci);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGridLicensableFeatures(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures) {
    if (!nvml_dispatch.nvmlDeviceGetGridLicensableFeatures) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGridLicensableFeatures(device, pGridLicensableFeatures);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGridLicensableFeatures_v2(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures) {
    if (!nvml_dispatch.nvmlDeviceGetGridLicensableFeatures_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGridLicensableFeatures_v2(device, pGridLicensableFeatures);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGridLicensableFeatures_v3(nvmlDevice_t device, nvmlGridLicensableFeatures_t * pGridLicensableFeatures) {
    if (!nvml_dispatch.nvmlDeviceGetGridLicensableFeatures_v3) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGridLicensableFeatures_v3(device, pGridLicensableFeatures);
}

static inline nvmlReturn_t dispatch_nvmlDeviceRemoveGpu(nvmlPciInfo_t * pciInfo) {
    if (!nvml_dispatch.nvmlDeviceRemoveGpu) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceRemoveGpu(pciInfo);
}

static inline nvmlReturn_t dispatch_nvmlEventSetWait(nvmlEventSet_t set, nvmlEventData_t * data, unsigned int timeoutms) {
    if (!nvml_dispatch.nvmlEventSetWait) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlEventSetWait(set, data, timeoutms);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetAttributes(nvmlDevice_t device, nvmlDeviceAttributes_t * attributes) {
    if (!nvml_dispatch.nvmlDeviceGetAttributes) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetAttributes(device, attributes);
}

static inline nvmlReturn_t dispatch_nvmlComputeInstanceGetInfo(nvmlComputeInstance_t computeInstance, nvmlComputeInstanceInfo_t * info) {
    if (!nvml_dispatch.nvmlComputeInstanceGetInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlComputeInstanceGetInfo(computeInstance, info);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetComputeRunningProcesses(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v1_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetComputeRunningProcesses) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetComputeRunningProcesses(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetComputeRunningProcesses_v2(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v2_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetComputeRunningProcesses_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetComputeRunningProcesses_v2(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGraphicsRunningProcesses(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v1_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetGraphicsRunningProcesses) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGraphicsRunningProcesses(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGraphicsRunningProcesses_v2(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v2_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetGraphicsRunningProcesses_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGraphicsRunningProcesses_v2(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMPSComputeRunningProcesses(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v1_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetMPSComputeRunningProcesses) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMPSComputeRunningProcesses(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetMPSComputeRunningProcesses_v2(nvmlDevice_t device, unsigned int * infoCount, nvmlProcessInfo_v2_t * infos) {
    if (!nvml_dispatch.nvmlDeviceGetMPSComputeRunningProcesses_v2) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetMPSComputeRunningProcesses_v2(device, infoCount, infos);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetGpuInstancePossiblePlacements(nvmlDevice_t device, unsigned int profileId, nvmlGpuInstancePlacement_t * placements, unsigned int * count) {
    if (!nvml_dispatch.nvmlDeviceGetGpuInstancePossiblePlacements) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetGpuInstancePossiblePlacements(device, profileId, placements, count);
}

static inline nvmlReturn_t dispatch_nvmlVgpuInstanceGetLicenseInfo(nvmlVgpuInstance_t vgpuInstance, nvmlVgpuLicenseInfo_t * licenseInfo) {
    if (!nvml_dispatch.nvmlVgpuInstanceGetLicenseInfo) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlVgpuInstanceGetLicenseInfo(vgpuInstance, licenseInfo);
}

static inline nvmlReturn_t dispatch_nvmlDeviceGetDriverModel(nvmlDevice_t device, nvmlDriverModel_t * current, nvmlDriverModel_t * pending) {
    if (!nvml_dispatch.nvmlDeviceGetDriverModel) return NVML_ERROR_FUNCTION_NOT_FOUND;
    return nvml_dispatch.nvmlDeviceGetDriverModel(device, current, pending);
}


#endif /* NVML_DISPATCH_H */
